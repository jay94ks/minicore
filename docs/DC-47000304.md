# DebugGetRegisters/SetRegisters - 정지된 유저 Task의 전체 레지스터 스냅숏 위치 미확정

<!--
  이 파일은 자동 생성된 사본(캐시)입니다 - 손으로 편집하지 마세요.
  정본은 claude-native-workflow(CNW)의 DB에 있습니다.
  trackingCode: DC-47000304
  status: approved
  updatedAt: 2026-09-17T09:33:30.400Z
  갱신: docs cache sync cmtzsjm5c000fo401iozcc60t docs
-->

## 배경

`SP-9A6D579F`(프로세스 디버깅 서브시스템) §3.5는 `DebugGetRegisters`/`DebugSetRegisters`(RM-48E1E610 Debug 그룹 call 5/6)를 요구하지만, `PN-87D6B615`(구현 계획) 진행 중 이 두 syscall을 실제로 구현하려 하자 설계 공백을 만났다.

같은 증분에서 함께 구현된 `DebugContinue`/`DebugReadMemory`/`DebugWriteMemory`는 각각 (1) 스케줄러 상태(`TaskState`)만 건드리거나, (2) 대상 주소공간을 `Paging::translatePage()`로 직접 순회하는 커널 대행 복사라 레지스터 프레임 위치와 무관하게 구현 가능했다. 반면 `DebugGetRegisters`/`DebugSetRegisters`는 "정지된 디버기 Task가 트랩 당시 갖고 있던 **전체** 유저 레지스터 스냅숏"(콜러세이브 아닌 레지스터 포함 - RAX/RCX/RDX/RSI/RDI/R8-11/RIP/CS/RFLAGS/RSP/SS 등, SDM의 인터럽트 스택 프레임 + 트랩 진입 시 저장되는 GPR 전부)을 읽거나 써야 하는데, **이 값이 정확히 그 Task의 커널 스택 어느 오프셋에 있는지가 이 코드베이스 어디에도 확정돼 있지 않다.**

## 왜 기존 메커니즘으로 못 채우는가

- `kContextSwitch`(소프트웨어 컨텍스트 스위치, task.h)가 저장하는 건 콜리세이브 레지스터(rbx/rbp/r12-r15)+RFLAGS뿐이다 - 이건 "스케줄러가 Task를 다른 Task로 바꿔치기할 때"의 최소 저장 세트지, "이 Task가 ring3에서 트랩될 당시의 전체 레지스터"가 아니다.
- `SP-83A07867`("코어 소유 레지스터 디스패치 동기화" - CR3/RSP0/FPU/디버그 레지스터)가 다루는 건 "코어에 물려 있는 상태"(디스패치 시점에 다시 씀)뿐이다 - "이 Task 자신이 트랩 당시 가졌던 InterruptFrame의 정확한 스택 오프레벨"과는 범주 자체가 다르다.
- `DebugSetSingleStep`(RFLAGS.TF)도 정확히 같은 이유로 이미 미구현 상태로 남아 있다(`debug_session.h` 기존 주석, PN-87D6B615 "남은 범위" 항목1) - 이번에 발견한 공백은 그 문제의 상위 일반화(RFLAGS 하나가 아니라 전체 레지스터 세트)에 해당한다.

## 확인이 필요한 이유 (CLAUDE.md 규칙 4)

디버기가 하드웨어 브레이크포인트(#DB)로 정지하면 `kHandleUserBreakpointHit`(idt.cpp가 부르는 콜백)이 트랩 프레임(`InterruptFrame*`)을 이미 손에 쥐고 있지만, 그 시점 이후 `pausedByDebugger`만 세우고 반환하면(현재 구현) 그 프레임은 스택에서 사라진다 - `DebugGetRegisters`가 나중에(다른 syscall 호출, 다른 코어일 수도 있음) 그 값을 다시 읽으려면 어딘가에 그 프레임(또는 그 사본)을 **명시적으로 보관**해야 하는데, 다음 중 어느 방식으로 할지는 설계 결정이 필요하다:

## 후보

- **(A) `DebugSession`(또는 `Process`)에 레지스터 스냅숏 사본을 직접 저장** - `kHandleUserBreakpointHit`이 정지 판정 시 `InterruptFrame*`을 통째로(또는 필드별로) `DebugSession::savedRegisters`류 필드에 복사해 둔다. 간단하지만 정지될 때마다 매번 복사 비용이 들고, `DebugSetRegisters`가 값을 바꿔도 그 Task가 실제로 재개(`DebugContinue`)될 때 그 바뀐 값을 다시 실제 iretq 프레임에 반영하는 별도 배선이 필요하다.
- **(B) 커널 스택 위 InterruptFrame의 위치를 계산해 직접 가리킴** - 복사 없이 그 Task의 커널 스택에서 트랩 프레임이 항상 있는 고정 오프셋(또는 알려진 규칙)을 계산해 포인터로 직접 읽고 쓴다. 복사가 없어 `DebugSetRegisters`가 그대로 반영되지만, "이 Task의 커널 스택 맨 위에 정확히 어떤 순서로 무엇이 쌓여 있는지"(IST 사용 여부, 중첩 인터럽트 가능성 등)를 이 코드베이스가 지금 얼마나 안정적으로 보장하는지 확인이 필요 - 잘못 짚으면 대상 프로세스와 무관한 스택 메모리를 읽고 쓰게 된다.

  **[참고, 2026-09-17, minicore-f8 문서 리뷰]** `#DB`(vector 1)는 이미 `SP-677210E6`(TSS/IST) "IST 슬롯 배정" 표에 따라 **IST4로 고정 배정**돼 있다(`gIdt[1].ist = 4`, `gIstStacks[3][coreIndex][8192]` - 코어별 전용 8KiB 스택, 매 진입마다 RSP가 그 스택의 고정 꼭대기 주소로 강제 초기화됨, IST1(#DF)/IST2(NMI)에서 이미 실측 검증된 것과 동일 메커니즘, §"IST 슬롯 배정"/"실측 검증" 4번 참고). 이는 (B)의 위험을 상당히 낮추는 근거다 - "Task의 커널 스택"이 아니라 "그 코어의 IST4 전용 스택"이 고정된 위치이므로, InterruptFrame이 항상 그 스택 꼭대기 근처 **고정 오프셋**에 있다는 보장은 이미 성립한다(추가 검증 불필요). 다만 IST는 진입마다 RSP를 항상 같은 꼭대기 주소로 재설정하는 방식이라 **진짜 중첩(같은 코어에서 #DB 핸들러가 반환하기 전에 또 #DB가 발생)이 일어나면 이전 프레임을 덮어쓴다** - #DB ISR(SP-677210E6 "#DB ISR 핵심 라우팅")이 짧고 빠르게 반환하는 한 이 위험은 낮지만, (B)를 채택한다면 이 재진입 배제를 명시적으로 보장(예: 콜백 안에서 DR7 일시 비활성화, 또는 재진입 카운터로 감지해 거부)할지는 착수 시 결정이 필요하다.
- **(C) 지금은 보류, 실제 initrd 기반 E2E 디버깅 시나리오가 필요해질 때까지 미착수** - `DebugContinue`/`DebugReadMemory`/`DebugWriteMemory`만으로도(레지스터 조회/수정 없이) 브레이크포인트 정지→메모리 조회→재개라는 최소 디버깅 왕복 자체는 이미 가능하다.

## [확정, 2026-09-17, 설계자 답변 QU-C82E903B] (A) DebugSession에 레지스터 사본 저장 채택

설계자 답변: "(A) DebugSession에 레지스터 사본 저장 = 인터럽트 그
자체가 다른 프로세스에게 실행 기회를 줘야 하는데 다른 방안을
사용하면 그렇게 할 수가 없어" - (B)(커널 스택 위 프레임을 직접
계산해 포인터로 접근)는 그 프레임이 "다른 프로세스가 실행될 수
있도록" 인터럽트 자신이 EOI 후 즉시 다음 Task로 넘어가는 흐름과
양립하지 않는다고 판단해 배제, (A)를 확정.

## [완료, 2026-09-17, PN-87D6B615] 구현

이 커널의 소프트웨어 컨텍스트 스위치(`kContextSwitch`)는 커널 스택
"내용"을 옮기지 않고 스택 포인터 자체를 스위칭하므로, `Scheduler::
onTick()`이 이 Task를 `Blocked`로 남기는 바로 그 순간 손에 쥔
`InterruptFrame*`(그 Task 자신의 커널 스택 위, isr_common_stub이
쌓아 둔 자리)가 이 Task가 재개될 때까지 정확히 그 자리에 그대로
살아있다는 점(다른 무엇도 그 스택을 건드리지 않음)을 이용했다 -
`kSaveDebugRegistersSnapshot(Task*, InterruptFrame*)`(debug_session.h/
.cpp 신규)가 그 프레임의 필드별 값을 `DebugSession::savedRegisters`
(값 사본)에 복사해 두고, 동시에 그 살아있는 프레임 자신의 주소를
`DebugSession::liveFramePtr`(내부 전용, 어떤 syscall args에도 노출
안 됨)에 남긴다. `Scheduler::onTick()`이 `pausedByDebugger`로
Blocked 판정할 때 이 함수를 호출하도록 배선했다(scheduler.cpp,
onTick의 `InterruptFrame*` 매개변수를 처음으로 실제 사용).

`DebugGetRegisters`/`DebugSetRegisters`(RM-48E1E610 call 5/6, 신규
`DebugGetRegistersHandler`/`DebugSetRegistersHandler`)는 `savedRegisters`
사본만 읽고 쓴다 - `DebugSetRegisters`가 바꾼 값은 `DebugContinue`가
재개 직전 `liveFramePtr`에 다시 써넣어(write-back) 실제 iretq
프레임에 반영하고, 그 직후 `liveFramePtr`를 `nullptr`로 되돌려
재사용/댕글링을 막는다. QEMU PVH 단일코어 TEMP 11/11 검증(스택
로컬 `InterruptFrame`으로 실제 #DB/onTick() 없이 `kSaveDebugRegistersSnapshot`
직접 호출 - kernel-space 값 스냅숏/조회/수정/write-back/liveFramePtr
초기화까지 전부 확인). SMP4는 별개의 사전 존재 버그(`PN-9F8FF132`)
때문에 이 TEMP 자체는 실행하지 않았으나, production 코드(핸들러
등록 + onTick 배선)만 있는 상태로는 GRUB SMP4 무회귀 확인.

## 참고

`PN-87D6B615`(구현 계획), `SP-9A6D579F` §3.5, `PN-9F8FF132`(SMP4
E2E 검증을 막는 별개의 사전 존재 버그).

