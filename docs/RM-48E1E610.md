# Minicore Syscall 할당표

<!--
  이 파일은 자동 생성된 사본(캐시)입니다 - 손으로 편집하지 마세요.
  정본은 claude-native-workflow(CNW)의 DB에 있습니다.
  trackingCode: RM-48E1E610
  status: review
  updatedAt: 2026-09-26T14:44:50.888Z
  갱신: docs cache sync cmtzsjm5c000fo401iozcc60t docs
-->

# Minicore Syscall 할당표

설계자 지시(2026-09-14, 메시지) - "Syscall ID 할당에 관한 사항도
`Minicore Syscall 할당표`에 정리해둬." `SyscallRegistry`(PL-21344323,
`minicore/kernel/syscall.h`)가 `SyscallEndpointId`(`uint32_t`)를
등록·해석하는 구조다 - 여러 설계 제안 문서가 각자 새 syscall을
"SyscallRegistry에 등록"이라고 언급만 하고 실제 번호는 비워 뒀는데,
이대로 각자 구현 착수하면 번호가 겹칠 수 있다. 이 문서가 **실제
번호를 미리 예약해 두는 단일 출처** 역할을 한다 - RM-7C249618
("Minicore 라이브러리 목록")/RM-C65F7760("Minicore VFS 구조")과
같은 성격의 "현재 상태를 추적하는 현황판" 문서다(설계 문서 자체가
아님).

## [완료, 2026-09-17] Group+Call 2단계 인코딩으로 전면 개편

설계자 지시(2026-09-17, opinion) - 기존의 평평한 0-255 번호 공간을
**그룹(비트 15:8) + 그룹 내 call 번호(비트 7:0)** 2단계로 재편했다
(`SP-E9B44929`, approved, `PN-B522DFEE`로 구현 완료·commit 반영).
비트 31:16은 예약(반드시 0). `SyscallEndpointId` 자체는 여전히
`uint32_t`(와이어 ABI 무변경) - 해석만 바뀐다.

```cpp
// syscall.h
constexpr SyscallEndpointId kMakeSyscallEndpointId(uint8_t group, uint8_t call);
constexpr uint8_t kSyscallGroupOf(SyscallEndpointId id);
constexpr uint8_t kSyscallCallOf(SyscallEndpointId id);
```

**이 문서가 계속 중재하는 것은 그룹 번호(0-255)뿐이다** - 그룹
안의 call 번호는 그 그룹을 소유한 subsystem 설계 문서가 다른
그룹과 조율 없이 0부터 독자적으로 채운다(CLAUDE.md 규칙 10의
"번호부터 예약" 정신은 그룹 단위로 적용). 이 문서는 그래도 "전체
그룹 + 각 그룹의 현재 call 목록"을 한눈에 보여주는 현황판 역할은
유지한다(SP-E9B44929 §5).

**2026-09-16 "빈칸 재사용" 정책은 그룹 재편으로 의미가 사라졌다** -
전역 번호 공간 자체가 없어졌으므로, 폐기된 call 번호를 재사용할지는
이제 그 call이 속했던 그룹의 소유 문서가 독자적으로 판단한다(예:
과거 36/37/46번 자리였던 `UnpublishInterface`/`QueryInterfaces`/
`GetKernelServiceRingBuffer`는 전부 그룹 재편 이전에 이미 폐기된
것들이라 새 그룹표에 자리 자체가 없다 - 해당 그룹 소유 문서가 필요
시 그 그룹 안에서 새 call로 재도입 여부를 판단).

## 기록 규칙

- 새 syscall을 설계 문서에 추가할 때마다, 그 그룹의 call 목록에서
  다음 미사용 call 번호로 행을 추가한다(구현 여부와 무관 - 제안
  단계에서도 번호부터 예약).
- 새 그룹이 필요하면(기존 9개 그룹 중 어디에도 속하지 않는 완전히
  새로운 subsystem) 이 문서의 그룹 배정 표에 다음 미사용 그룹
  번호로 행을 추가한다.
- "상태" 칸: `구현 완료`(실제 `SyscallRegistry::registerHandler` 호출
  코드가 있음) / `번호만 예약`(설계 문서에 구조체는 있으나 아직
  미구현).

## 그룹 배정

| 그룹 | 이름 | 소유 문서 |
|---|---|---|
| 0 | Process | SP-04EE2A18/SP-0666DB3C/SP-6BEAE0C1 |
| 1 | Channel | SP-1FBC0EEB |
| 2 | Device | SP-9DD4F3EA/SP-39F18E30 |
| 3 | Vfs | SP-7CC5693A/SP-2AAD7C8D §9.3 |
| 4 | Memory | SP-2AAD7C8D §5 |
| 5 | Interrupt | SP-71DA77B3 |
| 6 | Event | SP-2602CAA6 |
| 7 | Debug | SP-9A6D579F |
| 8 | Sync | SP-0666DB3C §17 |
| 9 | ResourceGroup | SP-245D130B/SP-6A563A8F |
| 10 | Power | SP-0C7A4F3B |
| 11 | Socket | SP-231493CB |

다음 미사용 그룹 번호: **12**.

## 그룹 11 - Socket (번호만 예약, SP-231493CB review 중)

| call | 이름 | 상태 |
|---|---|---|
| 0 | Socket | 번호만 예약(SP-231493CB §5) |
| 1 | Bind | 번호만 예약(SP-231493CB §5) |
| 2 | Listen | 번호만 예약(SP-231493CB §5) |
| 3 | Accept | 번호만 예약(SP-231493CB §5) |
| 4 | Connect | 번호만 예약(SP-231493CB §5) |
| 5 | Shutdown | 번호만 예약(SP-231493CB §5) |

다음 미사용 call: 6.

(epoll류 설계 `SP-6350DEBB`는 새 그룹을 만들지 않고 그룹6(Event)의
다음 미사용 call 번호를 이어 쓴다 - 아래 그룹6 절 참고.)

## 그룹 0 - Process

| call | 이름 | 상태 |
|---|---|---|
| 0 | SelfTerminate | 구현 완료 |
| 1 | Kill | 구현 완료(PN-71E50394 항목4, 임의 대상 확장 PN-88E62419) - `kResolveProcessId()`로 임의 프로세스를 대상으로 지정 가능, 권한은 커널/KernelService 예외 → 직계 부모 예외 → uid/gid RWX(`kCheckPermission`, SP-30FCC8AE) 순서로 판정 |
| 2 | SignalAction | 구현 완료(PN-71E50394 항목4) |
| 3 | Wait | 구현 완료 - v1은 논블로킹(POSIX `waitpid(pid, status, WNOHANG)`과 동일한 의미) |
| 4 | SpawnProcess | 구현 완료(PN-543C0CE9) - argv/envp 실제 전달, `flags: uint32_t` 파라미터 |
| 5 | SetTaskWeight | 구현 완료(`PN-158B6B2F`, commit `01f5c59`) - `targetPid`(자기 자신만 이번 증분, `kSelfTaskWeightPid=-1`) + `weight`([-100,100], 기본 0). 직계 자식 대상은 `SP-30FCC8AE`(uid/gid) 승인 이후 별도 후속 증분 |
| 6 | CreateThread | 구현 완료(`PN-0EB2FABF` 2단계, commit `02b2702`) - 호출자와 같은 프로세스 안에 스레드 추가, 새 주소공간 없이 pml4Phys 공유 + mapRegion()으로 스택만 확보. **알려진 한계**: ProcessAddressSpaceManager가 최대 8개 VMA만 지원해 실제 스레드 상한은 `kMaxThreadsPerProcess`(256)보다 훨씬 낮다 - mapRegion() 실패는 OutOfMemory로 거부 |
| 7 | Join | 구현 완료(`PN-0EB2FABF` 4단계, commit `5ee0fec`) - 진짜 블로킹(대상이 이미 좀비면 즉시 회수, 아직 살아있으면 `co_await`로 정지했다가 `SelfTerminateThread`가 대신 재개). 이 커널 최초의 실제 `co_await` suspend/resume 사례 - 실측 중 `AsyncReactor::drainOnce()`의 코루틴 재개 경로가 CR3를 동기화하지 않던 잠재 버그(PN-523B779F가 예고해 둔 "남은 범위")를 발견해 수정 |
| 8 | Fork | 구현 완료(`PN-44C91D6E`) - COW 기반 `fork()`. `int 0x80` 경로에서만 지원(`kSyscallVerbFork`, `syscall` 명령 경로는 자식 재개에 필요한 GPR 스냅샷이 없어 도달 불가). 2회 시도의 크래시 원인이었던 스케줄러 버그가 `PN-D8AF3536`으로 해소된 뒤 3차 시도로 완료 - devmgr TEMP 하네스로 단발/지속 쓰기 두 시나리오 각각 30/40회 무크래시 검증 |
| 9 | SelfTerminateThread | 구현 완료(`PN-0EB2FABF` 3단계, commit `fd01642`) - `SelfTerminate`(0번)의 스레드 전용 대칭(POSIX `pthread_exit()` 의미). idt.cpp가 self-terminate와 동일하게 특별 취급(절대 ring3 미복귀). 자기 전용 유저 스택은 Join 여부와 무관하게 즉시 unmapRegion() - 8-VMA 희소 자원이라 |
| 10 | Detach | 구현 완료(`PN-0EB2FABF` 4단계, commit `5ee0fec`) - 논블로킹, 대상의 `detached` 플래그만 세운다. 옛 번호 8이 `Fork`와 충돌해 10으로 재배정된 이력 있음(`PN-44C91D6E`) |
| 11 | Setuid | 구현 완료(2026-09-23, `PN-B6DB692C`, `SP-30FCC8AE` §1-A) - uid는 트리 구조(root는 임의 uid로, 그 외는 authmgr read-through 캐시(`user_record.h`)의 parentUid 체인상 자신의 하위로만) - 판정+실행을 한 번에 하는 시도-실패 패턴(별도 조회 API 없음). **범위 축소**: authmgr 비동기 질의(캐시 미스 시 co_await) 배선은 다음 증분 - 지금은 캐시 미스=`ChannelError::ServiceUnavailable` 즉시 반환. gid는 이 syscall이 건드리지 않음(POSIX 관례) |

다음 미사용 call: 12.

## 그룹 1 - Channel

| call | 이름 | 상태 |
|---|---|---|
| 0 | OpenChannel | 구현 완료 |
| 1 | ConnectChannel | 구현 완료 |
| 2 | AcceptFromChannel | 구현 완료 |
| 3 | ChannelRead | 구현 완료 |
| 4 | ChannelWrite | 구현 완료 |
| 5 | CloseBridge | 구현 완료 |
| 6 | DestroyChannel | 구현 완료 |

다음 미사용 call: 7.

## 그룹 2 - Device

| call | 이름 | 상태 |
|---|---|---|
| 0 | EnumerateDevices | 구현 완료(PN-BD9AAE2F 1번째 증분, commit 01d23ac) - 커널이 부팅 시 캐시한 PCI 토폴로지(BAR 포함, 헤더타입 0x00만)를 devmgr에 페이지네이션으로 반환. 실제 컴파일된 유저 프로그램이 이 syscall을 호출하면 PN-523B779F(스택 최상단 근처 Page Fault로 PANIC)에 걸려 end-to-end 검증은 아직 막혀 있음 |
| 1 | RequestIoPermission | 구현 완료(PN-BD9AAE2F 4번 항목) - `DeviceOwnerTable`(BAR 소유권, teardown hook 미연동은 PN-7528A406)로 중복 요청 거부, `ProcessAddressSpaceManager::mapRegion(VmaBacking::FixedPhysical)`로 매핑. MSI/MSI-X 벡터 배정·실제 BAR 크기 조회는 PN-7528A406으로 분리(v1은 항상 assignedIrqVector=0, 고정 4KiB) |
| 2 | AllocDmaBuffer | 구현 완료(PN-4E6EA13D 선행 작업, commit 0dc91c4, SP-39F18E30 §2/§3.1) - AHCI/USB 드라이버 자식이 컨트롤러 DMA 구조체용 물리 연속 메모리를 확보 |
| 3 | FreeDmaBuffer | 구현 완료(위와 동일, commit 0dc91c4) |

다음 미사용 call: 4.

## 그룹 3 - Vfs

| call | 이름 | 상태 |
|---|---|---|
| 0 | Mount | 구현 완료(PN-452FF696 항목2, commit 69d7fa9) - fs 서비스 → 커널 마운트 등록. `MountTable::mount()`가 "이미 마운트됨"과 "테이블 가득 참"을 구분 안 해 둘 다 AlreadyExists로 뭉뚱그림(정직하게 기록, vfs_syscall.h 참고) |
| 1 | Unmount | 구현 완료(PN-452FF696 항목2, commit 69d7fa9) |
| 2 | ResolvePath | 구현 완료(PN-452FF696 항목2, commit 69d7fa9) - `MountKind::KernelDriver` 대상(예: livefs)은 NotSupported로 응답(SP-2AAD7C8D §9.1 미해결 질문 - §9 Open/Read 착수 시 확정 예정, 임의 결정 아님) |
| 3 | SignalUserlandReady | 구현 완료(PN-452FF696 항목4, commit 69d7fa9) - 전역 `AtomicU32` CAS(0→1) 게이트, 단 1회만 허용 |
| 4 | WaitForUserlandReady | 구현 완료(PN-452FF696 항목4, commit 69d7fa9) - `AsyncTask::yield()` 폴링 블로킹(이 프로젝트의 다른 "동기처럼 보이는 비동기 대기"와 동일 관례) |
| 5 | Open | 구현 완료(PN-EA4EE935, commit 6b5f9a3) - `MountKind::KernelDriver`(livefs 등) 마운트만 지원, `MountKind::Channel`(유저랜드 fs 서비스)은 §9.1 IPC 와이어 포맷 미정으로 `NotSupported` |
| 6 | Close | 구현 완료(PN-EA4EE935, commit 6b5f9a3) - 위와 동일 스코프 |
| 7 | Read | 구현 완료(PN-EA4EE935, commit 6b5f9a3) - 위와 동일 스코프. `/sys/live/proc/self/status` 실측 검증(Open→Read→Close 5회 반복 성공) |
| 8 | Write | 구현 완료(PN-EA4EE935, commit 6b5f9a3) - 위와 동일 스코프. 현재 유일한 KernelDriver인 livefs는 읽기 전용이라 항상 PermissionDenied 반환(설계대로) |
| 9 | Lseek | 구현 완료(Set/Current만, PN-E9960D10, commit ddb9ff5) - fd 테이블 offset의 순수 산술, KernelFsDriver 호출 불필요. `whence=End`는 정직하게 NotSupported(FileDescriptor가 원래 경로를 안 들고 있어 파일 크기 조회 불가 - 후속 증분). `/sys/live/proc/self/status`에 Lseek(Set,5)→Read로 실측 검증(bytesRead=82=87-5 정확히 일치) |
| 10 | Stat | 구현 완료(PN-238FD331, commit 7977c2c) - `MountKind::KernelDriver`만 지원(Open과 동일한 스코프). `/sys/live/proc/self/status` 실측 검증(size=87, Read의 bytesRead와 정확히 일치, 5회 반복 성공) |
| 11 | Readdir | 구현 완료(PN-770A28FB, commit 4e25c76/386ad0f/8165a78/이후 1건) - `MountKind::KernelDriver`만 지원. `SP-7CC5693A` §3.2와 정합된 "Open으로 얻은 fd에 인덱스 하나씩 스트리밍" 방식(`Process::FileDescriptor::offset`을 커서로 재사용) - `EnumerateDevices`류 배치 방식 대신 채택. `/sys/live` 루트(named/kernel/initrd.cpio/proc/resourcegroup 5개 고정 항목) + `named/`(`NamedObjectTable::getByIndex`)/`kernel/`(`KernelReservedTable::getByIndex`)/`proc/`(`ProcFs::readdir`, self/meminfo/uptime 3개 고정 항목)/`resourcegroup/`(`ResourceGroupFs::readdir`, 그룹 트리 전체를 루트부터 DFS pre-order로 평탄화해 인덱스로 순회 - 동적 그룹 생성/삭제와도 자연스럽게 맞음) 하위 나열까지 지원 - `proc/self` 자신의 안쪽(pid별 동적 목록)만 아직 후속(QU-764C5624 답변 대기). `LiveFs::onExec` 직접 호출로 실측 검증(루트 5회+EOF, named 1개 항목+EOF, kernel 실제 스폰된 devmgr/fs/pubreg 3개+EOF, proc `self`/`meminfo`/`uptime` 3개+EOF, resourcegroup `root`+동적 자식 1개 생성 후 2개+EOF - 전부 기대값과 일치) |
| 12 | Mkdir | 구현 완료(PN-CF030FC3, commit 28c703a) - `MountKind::KernelDriver`만 지원, `StatHandler`와 동일 골격. livefs는 읽기 전용이라 실제로는 항상 `PermissionDenied` 반환(설계대로, `Write`와 동일 상황) - 실측으로 확인 |
| 13 | Unlink | 구현 완료(PN-CF030FC3, commit 28c703a) - 위와 동일 |
| 14 | Rmdir | **[구현 완료, 2026-09-23, commit 1467b93, PN-9D6FE4B6]** `KernelFsOpCode`(9개 op) 중 유일하게 syscall 번호가 없던 것을 발견 - Mkdir/Unlink(12/13)와 같은 시점에 함께 노출됐어야 했는데 `PN-CF030FC3`가 빠뜨렸다(순수 누락, 의도적 제외 아님). `RmdirArgs`/`kSyscallEndpointRmdir`+`RmdirHandler`(`UnlinkHandler`와 동일 골격)를 `vfs_syscall.cpp`에 실제 등록 - `Fat32Driver::Rmdir`(빈 디렉터리 검증 포함, mkfs.vfat 이미지+mtools/fsck.vfat 실측)로 실사용 확인 |

다음 미사용 call: 14.

## 그룹 4 - Memory

| call | 이름 | 상태 |
|---|---|---|
| 0 | Mmap | 구현 완료(PN-012E8C1A, commit a569f27) |
| 1 | Munmap | 구현 완료(PN-012E8C1A, commit a569f27) |
| 2 | Brk | 구현 완료(PN-012E8C1A, commit a569f27) |

다음 미사용 call: 3.

## 그룹 5 - Interrupt

| call | 이름 | 상태 |
|---|---|---|
| 0 | SubscribeInterrupt | 구현 완료(PN-B3DD3D19) |
| 1 | WaitInterrupt | 구현 완료(PN-B3DD3D19) |
| 2 | UnsubscribeInterrupt | 구현 완료(PN-B3DD3D19) |
| 3 | GetInterruptDump | 구현 완료(PN-B3DD3D19) |

다음 미사용 call: 4.

## 그룹 6 - Event

| call | 이름 | 상태 |
|---|---|---|
| 0 | SubscribeEvent | 번호만 예약(SP-2602CAA6 §5) |
| 1 | UnsubscribeEvent | 번호만 예약(SP-2602CAA6 §5) |
| 2 | WaitEvent | 번호만 예약(SP-2602CAA6 §5) |
| 3 | EpollCreate | 번호만 예약(SP-6350DEBB §5, review 중) |
| 4 | EpollCtl | 번호만 예약(SP-6350DEBB §5, review 중) |
| 5 | EpollWait | 번호만 예약(SP-6350DEBB §5, review 중) |
| 6 | TimerfdCreate | 번호만 예약(SP-A7479F83 §3, review 중) |
| 7 | TimerfdSetTime | 번호만 예약(SP-A7479F83 §3, review 중) |
| 8 | SignalfdCreate | 번호만 예약(SP-A7479F83 §3, review 중) |
| 9 | SignalfdSetMask | 번호만 예약(SP-A7479F83 §3, review 중) |

다음 미사용 call: 10.

## 그룹 7 - Debug

| call | 이름 | 상태 |
|---|---|---|
| 0 | DebugAttach | 구현 완료(PN-87D6B615) - v1은 §3.2 권한 모델 중 "호출자가 직계 부모"인 경우만(KernelService 임의 대상 예외는 QU-764C5624 답변 대기) |
| 1 | DebugDetach | 구현 완료(PN-87D6B615) - 위와 동일한 v1 스코프 |
| 2 | DebugSetBreakpoint | 구현 완료(PN-87D6B615, 2026-09-17) - Execute/Write/ReadWrite 조건, 하드웨어 반영은 대상 다음 디스패치 시점(kSyncDebugRegs). #DB ISR 콜백은 B0-B3(하드웨어 브레이크포인트)와 BS(싱글스텝, call 3 참고) 둘 다 소비 |
| 3 | DebugSetSingleStep | 구현 완료(PN-87D6B615, 2026-09-17). [갱신, 2026-09-19, PN-06A7C439] `targetThread` 파라미터 추가 - 하드웨어는 안 건드리고 그 스레드(`UserThread::debugSingleStepPending`, 원래 `DebugSession::singleStepPending`에서 스레드별로 이전)만 세우거나 내림. 실제 RFLAGS.TF(0x100) 반영은 DebugContinue의 write-back 시점 - pending=true면 TF를 세우고 즉시 소비(false로 되돌림), false면 TF를 강제로 지움(싱글스텝 트랩 직후의 savedRegisters.rflags에 TF=1이 남아 있어도 무한 싱글스텝에 안 빠지도록) |
| 4 | DebugContinue | 구현 완료(PN-87D6B615, 2026-09-17) - 정지된 대상을 Blocked→Ready로. 그룹이 frozen이면 실제로 깨우지 않고 pausedByDebugger만 해제(ResourceGroup::thaw()가 나중에 대신 깨움). [갱신, 2026-09-19, PN-06A7C439] targetThread는 여전히 없음(의도적) - 정지된 스레드 전부를 한 번에 write-back+재개하는 all-stop→continue-all 시맨틱 유지 |
| 5 | DebugGetRegisters | 구현 완료(PN-87D6B615, 2026-09-17) - DC-47000304 (A) 채택(설계자 답변) 반영. [갱신, 2026-09-19, PN-06A7C439] `targetThread` 파라미터 추가 - `savedRegisters`/`liveFramePtr`이 `DebugSession`(process-wide)에서 `UserThread`(스레드별)로 이전됨에 따라 이제 그 스레드를 targetThread로 지목해야 조회 가능. Scheduler::onTick()/#DB ISR이 pausedByDebugger로 Blocked 판정할 때 kSaveDebugRegistersSnapshot()이 그 스레드의 InterruptFrame을 UserThread::debugSavedRegisters에 복사 - 이 핸들러는 그 사본만 조회. **[알려진 갭, PN-EA968DF0]** 여러 스레드가 근접한 시점에 같은 공유 브레이크포인트를 히트하면 스냅숏이 손상되는 미해결 동시성 버그가 실측으로 발견됨 |
| 6 | DebugSetRegisters | 구현 완료(PN-87D6B615, 2026-09-17) - DebugGetRegisters와 대칭. [갱신, 2026-09-19, PN-06A7C439] `targetThread` 파라미터 추가(위 DebugGetRegisters와 동일한 이유). 이 핸들러는 그 스레드의 사본만 갱신, 실제 반영은 DebugContinue가 재개 직전 liveFramePtr에 write-back |
| 7 | DebugReadMemory | 구현 완료(PN-87D6B615, 2026-09-17) - 대상 주소공간을 Paging::translatePage()로 직접 순회하는 커널 대행 복사(direct map 경유), 여러 페이지 경계 처리 포함 |
| 8 | DebugWriteMemory | 구현 완료(PN-87D6B615, 2026-09-17) - DebugReadMemory와 대칭(방향만 반대) |

다음 미사용 call: 9.

## 그룹 8 - Sync

| call | 이름 | 상태 |
|---|---|---|
| 0 | MutexCreate | 구현 완료(PN-E82744B1, commit f338ab1) - 세대 태그 슬롯 테이블 핸들(gUserMutexTable, user_sync.cpp) |
| 1 | MutexDestroy | 구현 완료(위와 동일) |
| 2 | MutexLock | 구현 완료(위와 동일) - AsyncMutex(YieldingPolicy)만 사용, 경합 시 AsyncTask::yield() |
| 3 | MutexUnlock | 구현 완료(위와 동일) - UserMutex::owner 소유자 검증, 불일치 시 NotOwner |
| 4 | SemaphoreCreate | 구현 완료(위와 동일) - gUserSemaphoreTable 독립 핸들 테이블 |
| 5 | SemaphoreDestroy | 구현 완료(위와 동일) |
| 6 | SemaphoreWait | 구현 완료(위와 동일) - 소유자 개념 없음(§16.2) |
| 7 | SemaphorePost | 구현 완료(위와 동일) |

다음 미사용 call: 8.

## 그룹 9 - ResourceGroup

| call | 이름 | 상태 |
|---|---|---|
| 0 | ResourceGroupJoin | 구현 완료(PN-4190BBD3, commit b74fbfc) - v1은 호출자 자신만 대상(임의 프로세스 지정은 범위 밖, resource_group.h 문서 주석 참고). 권한은 SP-245D130B §9-2 확정대로 "호출자가 그 그룹의 조상 체인 안에 있을 때만" |
| 1 | ResourceGroupCreate | 구현 완료(PN-4190BBD3, commit b74fbfc, SP-6A563A8F §5-A) - 이름은 트리 전체에서 전역 유일하게 강제(SP-245D130B §8 원안의 "형제간 유일"보다 넓힘 - kFindResourceGroupByName()이 평평한 조회를 전제하므로) |
| 2 | ResourceGroupDestroy | 구현 완료(PN-4190BBD3, commit b74fbfc, SP-6A563A8F §5-A) - 자식/멤버가 남아있으면 NotEmpty로 거부(Linux cgroup과 동일한 "비어있음 강제") |
| 3 | ResourceGroupSetCpuQuota | 구현 완료(PN-4190BBD3, commit b74fbfc, SP-6A563A8F §3/§5) - periodTicks==0이면 무제한. 계층적 강제(자식 합이 부모를 못 넘음, QU-3356AD2B §9-3)를 kValidateChildQuotaAgainstParent()로 검증. 재설정 시 사용량/주기 즉시 리셋(QU-8ED9EBD2 확정) |
| 4 | ResourceGroupFreeze | 구현 완료(PN-4190BBD3, commit b74fbfc, SP-245D130B §4) |
| 5 | ResourceGroupThaw | 구현 완료(PN-4190BBD3, commit b74fbfc, SP-245D130B §4) |
| 6 | ResourceGroupSetIoQuota | **구현 완료(2026-09-22, PN-DEC738B8, commit 405428e)** - `ResourceGroupSetCpuQuota`(call3)와 완전히 동일한 계약/권한 검증, byte 단위 쿼터 + 계층적 강제(`kValidateChildIoQuotaAgainstParent`). 실제 강제 지점은 `vfs_syscall.cpp`의 Read/Write 핸들러(드라이버 전달 직전, `kCheckAndConsumeIoQuota`) - `MountKind::KernelDriver` 경로(ext4/exfat/ntfs/FAT32/livefs/procfs 등) 전체에 적용된다. `MountKind::Channel`(`SP-2AAD7C8D` §9.1의 fs Channel IPC 와이어 포맷)은 여전히 미구현이라 그 경로의 실사용처는 없음(SP-A21DD889 §7이 이미 명시한 그대로) - 배선 자체는 마운트 종류 구분 없이 이미 끝났다. TEMP 단위 검증으로 `kCheckAndConsumeIoQuota`의 핵심 산술 확인, 표준 4시나리오 QEMU 회귀 무회귀 |

다음 미사용 call: 7.

## 그룹 10 - Power

| call | 이름 | 상태 |
|---|---|---|
| 0 | Shutdown | 구현 완료(2026-09-23, SP-0C7A4F3B, PN-0B461E6F) - 마운트된 볼륨 unmount 훅 호출 후 ACPI PM1 제어 레지스터에 SLP_TYPa\|SLP_EN을 써서 전원을 끈다. 실측: 이 프로젝트가 쓰는 QEMU `pc`/i440fx+SeaBIOS 머신에서 실제로 QEMU 프로세스가 종료됨(exit 0). |
| 1 | Reboot | 구현 완료(2026-09-23, SP-0C7A4F3B, PN-0B461E6F) - 마운트된 볼륨 unmount 훅 호출 후 ACPI Reset Register(지원 시) 또는 8042 컨트롤러 리셋으로 재시작한다. 실측: 이 머신은 Reset Register가 없어(FADT flags bit10 꺼짐) 8042 폴백이 실제 경로 - 마찬가지로 QEMU 프로세스 종료로 검증. |

다음 미사용 call: 2.

## 참고 - `waitForSyscall`/`waitForMultipleSyscall`/`waitAnyForMultipleSyscall`은 여기 없다

`Syscall::wait`/`waitForMultipleSyscall`/`waitAnyForMultipleSyscall`
(SP-04EE2A18, 이미 구현 완료)은 **`SyscallEndpointId`를 갖는 별도
syscall이 아니다** - 이미 제출된(submit된) syscall의 완료를 기다리는
커널 내부 API(AsyncTask 완료 대기)일 뿐이라 이 할당표의 대상이 아니다.
이 표는 오직 `SyscallRegistry::registerHandler`로 등록되는, 고유
`SyscallEndpointId`가 필요한 syscall만 다룬다.

