# Minicore 라이브러리 목록

<!--
  이 파일은 자동 생성된 사본(캐시)입니다 - 손으로 편집하지 마세요.
  정본은 claude-native-workflow(CNW)의 DB에 있습니다.
  trackingCode: RM-7C249618
  status: review
  updatedAt: 2026-09-24T08:02:34.576Z
  갱신: docs cache sync cmtzsjm5c000fo401iozcc60t docs
-->

# Minicore 라이브러리 목록

새 라이브러리(커널이든 유저랜드든)를 만들 때마다 이 문서에 항목을
추가한다(설계자 지시, 2026-09-14 - CLAUDE.md 규칙에도 명시).

## 커널 측 (minicore/libs/*)

**배치 원칙(2026-09-17, 설계자 의견으로 명시화)**: `minicore/libs/*`는
"커널 코드" 전용이 아니다 - 커널 자신뿐 아니라 **"커널 서비스"**
(devmgr/fs/net/tty/pubreg/authmgr 등, 전부 유저랜드에서 구동되지만
커널이 통제권을 쥔 프로세스, `SP-8B6B8D25` §2-A)의 의존성도 여기
둔다(예: `libkvdb` - authmgr 전용 소비자지만 커널 코드가 아님, 순수
유저랜드 라이브러리). 반대로 일반 최종 사용자 애플리케이션이 쓰는
라이브러리는 `userland/libs/*`(문서 하단)에 둔다 - 구분 기준은
"커널 코드 여부"가 아니라 "커널이 통제하는 서비스 계층의 의존성인가,
아니면 순수 사용자 애플리케이션 계층의 의존성인가"이다.

| 이름 | 경로 | 대상 | 용도 |
|---|---|---|---|
| libkenv | minicore/libs/libkenv | 커널 전용 | freestanding C/C++ 초기 런타임(표준 정수 타입, memcpy류, Spinlock/AtomicPtr, kernel::string, C++20 코루틴 지원, cxxabi) |
| libcpio | minicore/libs/libcpio | 커널/유저 공용(표준 C++ 내장 타입만 사용) | CPIO(New ASCII Format) 아카이브 파서 - initrd 읽기용(PL-FC38956C) |
| libkmm | minicore/libs/libkmm | 커널 전용(커널 API를 직접 참조) | Slab 할당자(SP-D7013B26, 코어별 더블 매거진+전역 디포) |
| libelf | minicore/libs/libelf | 커널/유저 공용(`MINICORE_LIBELF_KERNEL` 매크로로 컴파일 모드 구분, 기본값은 유저 영역용) | ELF64 파서 + 커널 전용 프로세스 로더(`loadIntoAddressSpace`) - PN-58501EAA |
| minicore_libs_x86_64 | minicore/libs/x86_64 | 커널 전용(`io_port.h`는 애초에 유저/커널 공용 전제로 설계됨, QU-4606360C - **[이전 예정, 2026-09-24] `RM-23F4B687` §3 지침 변경(QU-A2CABBC6)으로 `minicore/arch/x86_64`로 이전 + 유저/커널 공용화 예정, `PN-E612E714`(scheduled)** | x86_64 아키텍처 종속 코드 - `context_switch.S`(소프트웨어 컨텍스트 스위칭), `io_port.h`(포트 I/O), `isr.S`(ISR 저지대 스텁), `msr.h`(rdmsr/wrmsr), `syscall_entry.S`(syscall 진입점) |
| libext4 | minicore/libs/libext4 | **[정정, 2026-09-22, 착수 시 발견 - libswapfs와 동일한 사유]** 커널 전용(매크로 게이팅 없음) - `fs`가 `SP-43331889`로 순수 커널 KernelThread에 흡수된 뒤라 유일한 소비자가 커널 자신뿐, 실사용처가 생기면 그때 확장 | ext4 온디스크 포맷 읽기 라이브러리 + VFS 통합 계층 - **[갱신, 2026-09-22]** `PN-22784AD4`(1차 증분, commit 9049001) - `Ext4Volume::mount()`(슈퍼블록/그룹디스크립터, 리눅스 커널 소스와 1바이트 단위 대조, `GroupDesc32` 36→32바이트 정정 + FLEX_BG 허용 발견)까지 구현. **`PN-9AE5BFE4`(VFS 통합 계층, commit 64e9d49)** - `Ext4Driver : kernel::FileSystemDriver`(`SP-2BCE5D60` §3.1, `KernelFsDriver` 확장)의 `mount()`/`remount()`/`onExec()`(Open/Read/Stat/Readdir) 전부 구현 완료, 실제 mkfs.ext4 이미지로 `AsyncTask::submit()` 왕복까지 QEMU 검증(dumpe2fs/debugfs와 일치). **`PN-6EDED542`(코루틴 I/O 대기 메커니즘, commit bfc2f63)를 신규 도입** - `kernel::AsyncTaskCoroAwaiter`/`AsyncTask::waitingAsyncTask`(코루틴 onExec 안에서 다른 AsyncTask 완료를 안전히 기다리는 범용 프레임워크 primitive, `SP-F682B889` §9.5-3). `AsyncExecCoro` 자체는 코루틴 합성(다른 코루틴을 co_await)을 지원하지 않는다는 제약 때문에 `Ext4Volume`의 옛 동기 메서드(resolvePath/readInode/statInode/readdirAt)는 폐기하고 `Ext4Driver::onExec` 안에 평탄화해 재구현 - `Ext4Volume`엔 `mount()`와 상태 접근자만 남음. 쓰기 경로(mkdir/rmdir/unlink/write)와 후속 증분 6개(레거시 간접블록/저널 리플레이/메타데이터 체크섬/64bit/쿼터/htree 쓰기), 실제 AHCI 부팅 시퀀스 배선(`PN-A0CEF82D` 대기)은 아직 미구현.
| libswapfs | minicore/libs/libswapfs | **[정정, 2026-09-22, 착수 시 발견]** 커널 전용(매크로 게이팅 없음) - SP-D02C4A73/SP-2BCE5D60 §4가 세워질 당시 `fs`가 별도 유저랜드 프로세스라는 전제로 "커널/유저 공용"을 가정했으나, `SP-43331889`로 `fs`가 Process 없는 순수 커널 KernelThread로 이미 흡수된 뒤라(`block_device.h`/`swap_backend.h`와 동일한 사정) 실제로는 유일한 소비자가 커널 자신뿐이다 - `libelf`/`libcpio`류 이중 컴파일 매크로를 안 둠, 실사용처가 새로 생기면 그때 확장 | swapfs 온디스크 포맷 읽기/쓰기 라이브러리(위 정정과 동일 원칙 - fs 서비스가 통합, SP-7CC5693A §3.1a/§3.3) - **[갱신, 2026-09-22]** `PN-6D9A5DAE` 구현 완료(commit 34a0320) - `SwapHeaderInfo`(Linux swap-space v2 헤더, 매직 "SWAPSPACE2") + `SwapfsBackend`(mount/allocateSlot/freeSlot/readSlot/writeSlot, 런타임 전용 사용 비트맵) + `paging.h`의 `PAGE_SWAP_MARKER`/`kMakeSwapPte`/`kSwapSlotFromPte`. 실제 `mkswap`(util-linux 2.39.3) 이미지와 1바이트 단위 대조 + 리눅스 커널 소스(`mm/swapfile.c`)로 재검증해 설계 문서의 `info.version=2` 가정 오류를 잡아 `1`로 정정(매직 문자열만으로 SWAP-SPACE/SWAPSPACE2 구분, version은 별개의 "서브버전" 상수). QEMU 검증은 실제 AHCI 대신 real mkswap 헤더 바이트를 실은 메모리 목 `BlockDevice`로 수행 - 커널 모드 `KernelThread` 컨텍스트에서 AHCI 일반 READ/WRITE DMA 경로가 `mount()` 안에서 무한 대기하는 별도 버그를 발견(추적 예정, PN-6D9A5DAE 범위 밖). `Paging::handlePageFault()` 스왑 마커 분기와 스왑아웃 경로 배선은 rmap/reclaim 인프라 부재로 이번 범위에서 보류(계획 본문 참고) |
| libvfat | minicore/libs/libvfat | **[정정, 2026-09-22, 착수 시 발견 - libext4/libswapfs와 동일한 사유]** 커널 전용(매크로 게이팅 없음) - `fs`가 유일한 소비자, 실사용처가 생기면 그때 확장 | FAT32 온디스크 포맷 읽기 라이브러리 + VFS 통합 계층 - `PN-F32F55A8`(1차 증분, commit 60f2713) - 구조체 레이아웃을 Linux 커널 소스(`include/uapi/linux/msdos_fs.h`)와 1바이트 단위로 대조(설계 문서 자체엔 오류 없음). **[갱신, 2026-09-22] `PN-EBAEA67B`(VFS 통합 계층, commit 5090244)** - `Fat32Driver : kernel::FileSystemDriver`(`SP-2BCE5D60` §3.1)의 `mount()`/`remount()`/`onExec()`(Open/Read/Stat/Readdir) 전부 구현 완료 - `Ext4Driver`(`PN-9AE5BFE4`)가 먼저 확립한 `kernel::AsyncTaskCoroAwaiter`(`PN-6EDED542`) 기반 평탄화(flatten) 패턴을 그대로 적용, `Fat32Volume`의 옛 동기 헬퍼(resolvePath/readData/readdirAt)는 폐기하고 `Fat32Driver::onExec` 안에 재구현 - `Fat32Volume`엔 `mount()`와 상태 접근자만 남음. `FileHandle`은 firstCluster(하위 32비트)+fileSize(상위 32비트) 인코딩(FAT엔 ext4의 inode 같은 재조회 가능한 단일 식별자가 없어 착수 세션이 결정). 실제 `mkfs.fat -F32` 이미지로 `AsyncTask::submit()` 왕복까지 QEMU 4개 시나리오 전부 검증(open/read/stat/readdir 전부 일치) - `PN-F32F55A8` 때와 달리 이번엔 진짜 커널/QEMU 실행 환경에서 검증됨. FAT16/FAT12, LFN, 쓰기 경로, 후속 증분 6개(FSInfo 유지보수/타임스탬프/NT 대소문자/dirty 비트 등), 실제 AHCI 부팅 시퀀스 배선(`PN-A0CEF82D` 대기)은 아직 미구현. |
| libexfat | minicore/libs/libexfat | **[정정, 2026-09-22, 착수 시 발견 - libext4/libswapfs/libvfat과 동일한 사유]** 커널 전용(매크로 게이팅 없음) - `fs`가 유일한 소비자, 실사용처가 생기면 그때 확장 | exFAT 온디스크 포맷 읽기 라이브러리 + 무상태 API - **[갱신, 2026-09-22]** `PN-C93A4E9E`(1차 증분, commit 21e633b) - `ExfatVolume`(mount/resolvePath/readData/readdirAt) 구현 완료. Microsoft exFAT 스펙 + Linux 커널 소스(`fs/exfat/exfat_raw.h`)와 1바이트 단위로 대조(설계 문서 `SP-F1987EF8` 자체엔 레이아웃 오류 없었음 - `libvfat`의 FAT32와 같은 경우). 할당 비트맵/Up-case 테이블(0xFFFF 런렝스 압축 해제) 로딩, NoFatChain 최적화, 클러스터 경계를 넘나드는 디렉터리 엔트리 집합(0x85+0xC0+0xC1×N) 조립까지 포함. exFAT은 FAT32(40MB 최소)와 달리 최소 볼륨이 ~3MB라, 실제 `mkfs.exfat`+FUSE로 채운 이미지 전체를 커널 소스에 임베드해 진짜 QEMU 4개 시나리오 전부에서 검증(`libvfat`의 호스트 네이티브 컴파일 검증보다 더 강함). **`PN-09970F05`(VFS 통합 계층, commit df62c4c)** - `ExfatDriver : kernel::FileSystemDriver`(`SP-2BCE5D60` §3.1)의 `mount()`/`remount()`/`onExec()`(Open/Read/Stat/Readdir) 전부 구현 완료 - `Ext4Driver`/`Fat32Driver`가 확립한 `AsyncTaskCoroAwaiter` 기반 평탄화 패턴을 그대로 적용, `ExfatVolume`의 옛 동기 공개 API(resolvePath/readdirAt 등)는 제거하고 `ExfatDriver::onExec` 안에 재구현(NoFatChain 최적화 포함). `FileHandle`은 firstCluster(하위 32비트)+fileSize(31비트, v1 절단, 최대 ~2GiB-1)+noFatChain(최상위 1비트) 인코딩(FAT류와 같은 이유로 착수 세션이 결정 - exFAT의 64비트 dataLength를 손실 없이 담을 자리가 없어 절단 감수). 실제 `mkfs.exfat` 이미지(파일+서브디렉터리, NoFatChain 항목 포함)로 QEMU GRUB+AHCI 시나리오에서 진짜 `gAhciBlockDevice` + `AsyncTask::submit()` 왕복까지 검증(중첩 경로 해석 포함). 쓰기 경로, TexFAT/dirty비트/PercentInUse 후속 증분은 아직 미구현. |
| libntfs | minicore/libs/libntfs | **[정정, 2026-09-22, 착수 시 발견 - libext4/libswapfs/libvfat/libexfat과 동일한 사유]** 커널 전용(매크로 게이팅 없음) - `fs`가 유일한 소비자, 실사용처가 생기면 그때 확장 | NTFS 온디스크 포맷 읽기 전용 라이브러리 + 무상태 API - **[갱신, 2026-09-22]** `PN-49F24FD7`(1차 증분, commit b6461e3) - `NtfsVolume`(mount/resolvePath/readData/readdirAt) 구현 완료. `SP-AA6DF406`이 스스로 "셋 중 신뢰도가 가장 낮다"고 경고한 대로 Linux 커널 소스(`fs/ntfs3/ntfs.h`)와 재대조해 실제 정정 1건 발견(`NtfsNonResidentAttrTail::compressionUnit` 2+4바이트 → 1+5바이트, 값 오염은 없었음) + `$FILE_NAME`/`$STANDARD_INFORMATION`/`IndexHeader`(원래 설계 문서가 "미확정"으로 열어 둔 부분) 신규 확정. MFT fixup 검증, 상주/비상주 `$DATA`(데이터 런 디코딩 포함), `$INDEX_ROOT` 소형 디렉터리 열거, 대용량 디렉터리(`$INDEX_ALLOCATION` 필요) 정직한 거부까지 실제 `mkfs.ntfs`+`ntfs-3g` 이미지로 QEMU 4개 시나리오 전부 검증(단, 이 이미지의 루트 자체는 mkntfs 특성상 항상 large라 `resolvePath`의 다중 세그먼트 탐색은 하위 디렉터리 경유로만 간접 검증 - 완료 보고에 상세). **`PN-52C577F3`(VFS 통합 계층, commit d790fae)** - `NtfsDriver : kernel::FileSystemDriver`의 `mount()`/`remount()`/`onExec()`(Open/Read/Stat/Readdir) 구현 완료(읽기 전용, `remount(true)`는 명시적 거부) - MFT 레코드 번호를 `FileHandle`에 그대로 담는다(ext4의 inode 패턴과 동일, FAT류처럼 합성 인코딩 불필요). $INDEX_ROOT가 항상 MFT 레코드 안에 상주해 디렉터리 열거에 추가 클러스터 순회가 필요 없다는 점이 ext4/FAT류보다 평탄화를 단순하게 만들었다. **실측으로 SP-AA6DF406의 실제 오류를 하나 더 발견** - `kFileAttrDirectory`가 원안 0x10("FAT류 attr 비트와 값 호환")이 아니라 실제로는 0x10000000이어야 정확함(실제 mkfs.ntfs 이미지의 $FILE_NAME 중복 정보를 바이트 단위로 디코딩해 확인 - 디렉터리는 0x10 비트가 전혀 없고 0x10000000으로 표시됨, 고치지 않았다면 모든 하위 디렉터리를 파일로 오판했을 실제 결함). 실제 mkfs.ntfs+ntfs-3g 이미지로 QEMU GRUB+AHCI 시나리오에서 진짜 `gAhciBlockDevice` 왕복 검증(중첩 디렉터리 포함) - 단, mkfs.ntfs 기본값상 진짜 루트(레코드 5)가 파일 개수와 무관하게 항상 large directory라 Open()의 루트발 경로 해석 자체는 이 실제 디스크로 검증 불가(알려진 v1 범위 밖) - 실제 서브디렉터리 MFT 레코드 번호를 직접 FileHandle에 실어 동일 파싱 로직(Open/Stat과 완전히 같은 코드)을 왕복 검증했다. 쓰기 전체, `$INDEX_ALLOCATION` B+ 트리, `$LogFile` 리플레이, 압축/스파스/암호화/리파스포인트/ADS는 아직 미구현(1차 증분 자체가 읽기 전용으로 범위를 좁힘). |
| libjson | minicore/libs/libjson | 커널/유저 공용(**[정정, 2026-09-17, 착수 시 코드 감사]** 매크로 게이팅 없이 `libcpio`와 동일한 무조건부 스타일 - 커널 전용 API를 전혀 안 써서 게이팅할 대상 자체가 없었음, SP-CCACB192 착수 절 참고) | JSON 파서/직렬화(SAX 콜백, 동적 할당 없음) - **구현 완료**(commit 예정, `json.h`/`json.cpp`). **[갱신, 2026-09-18]** 애초 의도했던 소비자(`pubreg`의 register/query 프로토콜, PN-185406F6)는 `SP-B071E628` §6-6(QU-4B38857C 답변)로 완전 바이너리 고정 구조체로 전환돼 더 이상 이 라이브러리를 쓰지 않는다 - MCP 스타일 tool 선언의 JSON 문서화는 `RM-085694F8`/개별 응용 프로그램 문서 쪽 책임으로 재배치됐다. `libjson` 자체는 유효한 범용 라이브러리로 남아 있으나(다른 응용 프로그램이 자신의 tool 선언을 JSON으로 문서화할 때 재사용 가능), 지금 이 시점엔 실사용 소비자가 없다. **[해소, 2026-09-17, QU-6A72AFE6 설계자 답변]** 커널 툴체인을 `-mno-mmx -mno-sse -mno-sse2` + `-mcmodel=large`로 교체(cmake/toolchain-x86_64.cmake) - SP-CCACB192 §3의 "정수+부동소수점 전부 지원"이 실제 `double` 지원으로 완성됨(commit 586dcfc). 실측으로 좁힌 제약: x86-64 SysV ABI가 `double` 반환을 항상 XMM0로 강제해 "함수가 double을 값으로 반환"하는 것만 여전히 불가 - 매개변수/내부 계산은 x87로 정상 동작, `onNumber` 콜백이 `double`을 매개변수로 받는 형태로 복원. |
| libutf8 | minicore/libs/libutf8 | 커널/유저 공용(**[정정, 2026-09-17, 착수 시 코드 감사]** libjson과 동일한 이유로 매크로 게이팅 없음) | UTF-8 인코딩/유니코드 유틸(코드 포인트→UTF-8, 서로게이트 페어 조합) - `libjson`의 `\uXXXX` 이스케이프 디코딩이 최초 소비자(SP-CCACB192 §4/§6) - **구현 완료**(commit 예정, `utf8.h`/`utf8.cpp`). |
| libkcont | minicore/libs/libkcont | 커널/유저 공용(설계자 지시, 2026-09-17 - "커널/유저 공용으로 쓸 수 있도록 만들면 더 낫겠다") | 제네릭 컨테이너 템플릿(`Node`/`List`/`Vector`/`Rbtree`/`RbMultiTree`/`Map`/`OrderedList`/`LruList`/`Queue`) - 침습(intrusive)/비침습(non-intrusive) 두 갈래, 할당자는 함수 포인터 주입(libkmm 의존 회피), Policy(할당자/이동/소멸 훅)·Traits(Key/keyOf/Link 묶음) 템플릿 파라미터 메커니즘 도입(SP-FAF768AB §0-A/§0-B) - `minicore/kernel/kernel_container_policy.h`의 `KernelVectorPolicy`가 GenericSlabAllocator를 컴파일 타임에 고정한 "커널 최적화 버전" 첫 적용 사례 - 기반 9종 구현 완료(PN-633BF2D8, commit 6c7441e). `lockfree_vector.h`의 `LockFreeVector<T>`(append-only 세그먼트 방식, SP-4DCD0E6A §2)도 구현은 완료(PN-DAE91888, commit c0f35a0)됐으나 **[경고, 2026-09-17, 설계자 지시 - 근거 확정됨] 안전하지 않음으로 마킹됨 - 실 구현에 적용 금지**(세그먼트 할당 실패 시 이미 예약된 전역 인덱스가 영구 "구멍"으로 남는 결함 - 커널에선 할당 실패가 정상 조건이라 진짜 안전성 문제, RM-32D06563/PN-DAE91888 참고) - **[갱신, 2026-09-19]** `ConcurrentMap`/`ConcurrentRbtree`(PN-A8EF29F7)
도 구현 완료됐다(commit 1e45e20/53ad5db) - `libkcont`가 아니라
`minicore/kernel/concurrent_map.h`/`concurrent_rbtree.h`에 있다
(`LockFreeList`/`LockFreeQueue`와 같은 이유 - RCU 의존이 순수 커널
개념이라 커널/유저 공용 라이브러리에 못 둠). SMP4 동시성 스트레스
검증(4회 반복, failures=0)까지 완료. `LockFreeList`(Harris)/`LockFreeQueue`(Michael-Scott, 비침습으로 재설계됨 - QU-253D6D24)는 구현 완료(PN-013215F9, commit 7e6c438) - 단, **`minicore/libs/libkcont`가 아니라 `minicore/kernel/lockfree_list.h`/`lockfree_queue.h`에 있다**(RCU 의존이 순수 커널 개념이라 커널/유저 공용 라이브러리에 못 둠 - `KernelVectorPolicy`가 kernel/에 있는 것과 같은 이유, PN-013215F9 참고) |
| libkvdb | minicore/libs/libkvdb | 커널/유저 공용(SP-30FCC8AE §1-D, PN-24A2B6F5 §1-C/D 항목1 - "커널 구성요소가 아닌데 여기 배치되는 이유는 authmgr라는 커널 서비스의 의존성이기 때문") | 고정 용량 Key-Value 저장소(`kvdb::Store<MaxEntries, MaxKeyBytes, MaxValueBytes>`, 동적 할당 전혀 없음, 선형 탐색) - v1은 메모리 전용(재부팅 시 소실, 온디스크 포맷/libkcrypto AES256 암호화는 fs 영속화가 생긴 뒤 착수). **구현 완료**(commit 예정, `kvdb.h`, put/get/contains/erase/forEach) - 커널 컨텍스트(로깅 가능한 유일한 곳)에서 TEMP 자체 검증(표준 4시나리오 QEMU 회귀 포함) 후 원복, 실제 연결부는 아직 없음. authmgr(userland, 실사용처)이 UserRecord 조회/등록 프로토콜을 실제로 얹는 것은 PN-B6DB692C(UserRecord 스키마 확정, 아직 `scheduled` 아님) 이후 PN-24A2B6F5 후속 세션 몫. |

## 유저랜드 측 (userland/libs/*, userland/tests/*)

QU-FF3F0CAA 답변(2026-09-14)으로 확정된 디렉터리 체계. 빌드 트리 자체
(`userland/CMakeLists.txt` + `userland/cmake/toolchain-userland-x86_64.cmake`
+ 링커 스크립트, PN-4AD3CBE0 완료)는 준비됐고 검증용
`userland/tests/userlandtest`(순수 툴체인 검증 스텁, 라이브러리 아님)
까지 실제로 빌드됨:

| 이름 | 경로 | 용도 | 상태 |
|---|---|---|---|
| libmc | userland/libs/libmc | 커널 및 "커널 서비스"들의 API를 유저랜드에 노출(MiniCore 라이브러리) | 부분 구현 완료(PN-58269449 스캐폴딩 + PN-71C3D483로 `mc::selfTerminate()`가 실제 `int 0x80` 트랩 발동까지 완성 + PN-BD9AAE2F 3번 항목이 범용 `mc::submit()`/`mc::wait()`(제출/대기 분리 패턴 공용 진입점) 추가, 2026-09-17) - `submit()`/`wait()`는 아직 end-to-end 검증 미완(PN-523B779F 커널 PANIC 버그에 막힘). `pnp.h`(EnumerateDevices 유저랜드 ABI 거울)/`channel.h`(Channel IPC 거울, PN-EAB3A9AE)/`vfs.h`(VFS syscall 거울)/`pubreg.h`(pubreg register/query 와이어 포맷, SP-B071E628 §6-6, PN-185406F6)도 추가됨. **[신규, 2026-09-18, PN-10EE096A]** `waitAnyForMultipleSyscall()`/`waitForMultipleSyscall()`(ring3 verb=2) 추가 - 나머지 syscall 래퍼는 각 엔드포인트가 실제로 필요해지는 시점에 추가 |
| libcenv | userland/libs/libcenv | 유저랜드 프리스탠딩 런타임 최소 지원(`minicore/libs/libkenv`의 유저랜드 짝) | **구현 완료**(PN-185406F6 항목4 착수 중 발견/신설, commit 974adce) - memcpy/memset/memmove/memcmp(바이트 단위 단순 구현). 프리스탠딩 유저랜드 툴체인엔 `<string.h>`가 없어, 컴파일러가 구조체 대입/값초기화를 위해 암묵적으로 만들어내는 이 4개 심볼이 없으면 링크가 실패한다(pubreg의 연결/등록 테이블 코드가 처음으로 이 정도 크기의 구조체 대입을 씀) - `minicore/libs/libkenv/mem.cpp`와 완전히 동일한 구현을 유저 전용 빌드 트리에 복제(커널 라이브러리를 유저 실행 파일이 링크할 수 없어서). |
| libuserlandtest | userland/tests/libuserlandtest | 테스트용 유저 런타임 라이브러리 - shared/static 양쪽으로 빌드 가능해야 함(동적 라이브러리 지원 검증용) | 예정 |
| libcpio | userland/libs/libcpio | `minicore/libs/libcpio/cpio.cpp`를 유저랜드 빌드 트리에도 연결(out-of-tree 소스 재사용) - 그 라이브러리 자체는 이미 "커널/유저 공용"으로 설계돼 있었으나(위 커널 측 표 참고) 지금까지 유저랜드 쪽엔 실제로 연결된 적이 없었다 | **구현 완료**(PN-012D6310, 2026-09-22) - `/sys/live/initrd.cpio`(livefs가 노출하는 원본 CPIO 아카이브)를 유저랜드가 직접 파싱해 임의 ELF 바이트를 얻어 `SpawnProcess`에 넘기는 데 실제로 검증(TEMP 스캐폴딩)까지 마쳤다 - 현재는 permanent 소비자 없음, 다음 SpawnProcess 기반 유저랜드 스포너가 나타나면 재사용 |

## 기록 규칙

- 새 라이브러리 디렉터리(`minicore/libs/<name>` 또는
  `userland/{apps,libs,tests}/<name>`)를 실제로 만들 때마다 이 표에
  행을 추가한다 - 계획 단계(아직 코드 없음)는 "상태" 칸에 "예정"으로
  남겨 두고, 실제로 만들어지면 "완료"로 바꾼다.
- "대상" 칸에는 커널 전용/유저 전용/공용(매크로 구분 여부 포함)을
  명시한다.
- 이 문서를 다른 문서에서 인용할 때는 이 문서의 추적 코드를 그대로
  쓴다.

