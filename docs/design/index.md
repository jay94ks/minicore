# 설계 문서 색인

`docs/design/`의 모든 문서 목록. 설계 결정(ADR)은 하나의 거대한
로그 대신 **주제별로 분리된 파일**에서 관리한다 — 결정을 추가하거나
찾을 때는 아래 표에서 해당 주제의 파일을 먼저 찾는다.

## 설계 결정(ADR) — 주제별

| 문서 | 다루는 ADR | 내용 |
|---|---|---|
| [foundations.md](foundations.md) | ADR-001~003, 005~010, 042, 049, 132, 170, 182, 183, 188, 198, 200, 210, 220, 221, 225 | 프로젝트 목표, 아키텍처 범위, 언어, 코드 소유 경계, 네이밍 컨벤션, libmc(네이티브 유저랜드 C API 기반 라이브러리), M20 범위 좁힘(libc 채택 보류+libmc 최소 부분집합+커스텀 셸 대체 구현), M26 범위 좁힘(third_party/musl 채택+문자열 함수 부분집합만 실제 포팅, 전체 syscall 계층은 범위 밖), 실제 musl syscall 계층 포팅 전략(커널 확장 대신 musl의 syscall_arch.h를 패치해 유저랜드에서 우회, 정적 링킹만 지원), musl locale 범위(C/POSIX 고정), 네임스페이스 계층 컨벤션(kern::*·kernsrv::<서버명>·__internals__ 은닉·proto 예외, 규칙만 확정 — 기존 코드 적용은 미착수), mc(구 libmc)를 커널·유저 공용으로 통합(MC_LAND_KERNEL 매크로, uapi.hpp 폐지), M35 완성(musl setlocale() 왕복 — 계획 문서의 "미지원 로케일은 실패해야 한다"는 실제 musl 동작과 달라 검증 목표를 "성공하지만 ctype은 C 그대로"로 조정), M51 완성(익명 파이프/dup2 — 새 커널 프리미티브 대신 전용 서버 servers/pipesrv+재시도 폴링, ADR-183과 같은 전략의 pipe 적용), M52 완성(BusyBox 서드파티 도입 철회 후 실제로 완주 — 자체 작성 최소 셸 userland/msh+coreutils(echo/ls/cat), Linux ABI 초기 스택 실제 argv 전달(ADR-222)+memfs 동시성 버그(ADR-223)는 kernel-memory.md/kernel-ipc-objects.md 참고), M54 완성(msh 파이프라인/출력 리다이렉션 — execve()가 지우는 fd 테이블을 argv "@pipefd"/"@filefd" 관례로 우회+pipesrv 참조 카운트를 M51의 "dup" 계약 대신 "이동" 모델로 정정, 실행 중 참조 카운트 버그 두 겹 발견·수정) |
| [build-system.md](build-system.md) | ADR-019~022, 031, 091, 113, 115, 116, 125, 190, 195, 199, 213 | CMake, 툴체인, 저장소 구조, 서드파티 소스 관리, QEMU fork 정책, freestanding C++ 헤더 확보, freestanding memset/memcpy 제공, 디버그 심볼·QEMU GDB stub/진단 플래그, minicore 타깃 크로스 툴체인/SDK 내보내기(저장소 밖에서도 minicore 프로그램 빌드), 와이어 프로토콜 정의 방법론(서버 헤더의 @wire-op 마크업+Python 추출 도구), 자체 라이브러리 저장소 배치(libs/ 하위+lib 접두사 제거, libk→k/libmc→mc), M38 완성(export-sdk.py가 정적 libc.a/libmc.a만 내보냄 — 동적 링킹은 M29/ADR-203이 이미 정적으로 되돌아가 계획에서 빠짐, CMake 경로는 셸 스크립트 래퍼 대신 툴체인 파일이 clang을 직접 호출) |
| [kernel-ipc-objects.md](kernel-ipc-objects.md) | ADR-004, 011, 013, 015, 023, 028, 029, 032, 151, 155, 159, 161, 216, 223 | IPC 원시, 핸들·프록시 객체 모델, IPC의 cross-address-space 확장(서로 다른 유저 프로세스 간 실제 통신, sys_reply의 handles[] 지원, sys_recv/sys_reply syscall 노출), pages[] 페이로드의 cross-address-space 전달(수신자별 번역/공유매핑 이원화), 유저 프로세스 대상 매핑의 배치 위치+해제 시점(스레드별 고정 슬롯+자동 해제, M16 구현 완료 시점에 트리거를 "다음 deliver_message 목적지 선택 시점"으로 정정), M42 완성(서버 스레드의 재진입 IPC 왕복을 위해 thread::ipc.reply_target을 재진입 스택으로 확장 — svcmgr의 op_start가 처음으로 드러낸 진짜 교착 수정), M52 실행 중 발견("COPY" 페이지 전송이 실제로는 물리 프레임을 그대로 매핑하는 zero-copy라는 것 — 하나뿐인 서버 스크래치 버퍼(memfs::g_read_scratch)를 동시 다중 클라이언트가 공유해 진짜 데이터 손상+페이지 폴트로 이어짐, open 인스턴스별 버퍼 분리로 해결) |
| [kernel-memory.md](kernel-memory.md) | ADR-012, 016, 024, 033, 036, 051, 052, 054, 078, 104~108, 110, 121, 134, 136, 140~142, 145, 148, 149, 152, 160, 179, 180, 189, 202, 203, 204, 205, 207, 222 | 메모리 할당, 쿼터, NUMA, 락 모델(실제 락 순서 표 포함), 가상메모리 레이아웃, 메모리 예약·회수·압박 신호, 스왑 압축, 압축 실행 위치(SWAPFS 서버), 유저 주소공간의 저지대 항등 매핑(GDT) 공유, 슬랩 헤더 16바이트 정렬, COW 실제 구현(프레임 참조 카운트+PTE 소프트웨어 비트+쓰기 폴트 분기), syscall 커널 스택 스레드별 분리, sys_fork/sys_process_spawn/sys_exec/sys_thread_exit(레지스터 수준 fork 재현), pml4[0] 공유 범위를 [0,8MiB)로 정확히 좁힘, trusted 전용 DMA 버퍼 할당 syscall, M12 self_info 브릿지의 build_process() 일반화, sys_process_spawn 스폰 시점 캐패빌리티 주입, 유저 가상주소공간 전체 메모리맵 표 확정(고정 슬롯 크기+경계 검증으로 충돌 구조적 차단), M23 완성(sys_fork가 handle_table 전체를 프록시로 복제 — procsrv 매개 fd 프로토콜 없이 open_file_id 기반 서버측 상태로 오프셋 공유 성립), M24 완성(sys_brk 고정 1MiB 힙 슬롯+libmc 범프 할당자), 동적 링킹을 계획 초반으로 앞당김(PT_INTERP/auxv 지원+musl 자신의 공유 libc.so, 정적 링킹도 계속 지원), M28 실행 중 발견(musl 진짜 시작 경로가 arch_prctl/FS_BASE와 Linux ABI 초기 스택을 무조건 요구 — 둘 다 M30/M29에서 M28로 앞당김), M29 범위 좁힘(ELF 로더의 ET_DYN+bias 지원과 진짜 AT_PHDR/AT_ENTRY/AT_BASE auxv까지는 완료, musl 자신의 libc.so 빌드+자기재배치 부트스트랩은 위험도가 질적으로 달라 시도하지 않고 정적 링킹으로 계속 진행), M30 실행 전 발견(musl의 malloc이 우선 시도하는 SYS_brk가 libmc의 mc_malloc과 같은 커널 힙 상태를 공유해 캐시된 커서가 겹칠 수 있음을 확인 — SYS_brk를 항상 실패시켜 완전히 분리된 새 SYS_mmap 영역으로 강제 우회), M31 실행 중 발견(musl-hello를 커널 직접 스폰에서 VFS 의존 서비스로 재배치+새 mkbootdisk.py --linux-abi-stack= ini 키, vfprintf.c의 SSE 요구를 M9~M11b의 기존 유저 FPU 컨텍스트 스위칭으로 안전하게 수용, musl 자신의 Makefile과 같은 -D_XOPEN_SOURCE=700 필요), M32 실행 중 발견(fork()가 fs_base(TLS)도 물려줘야 함+sys_exec가 linux_abi_stack을 받도록 확장), M34 실행 중 발견(ADR-136의 락 목록 중 run_queue::lock/klog::g_log_lock이 실제로 상시 멀티코어 경합에 들어감 — irq_safe<Lock> 자체의 진짜 데이터 경합을 발견·수정), M52 실행 중 발견(execve()가 M28부터 argc=1/고정 argv[0]만 넘겨 실제 인자를 전달한 적이 없었음 — Linux ABI 초기 스택을 진짜 argc/argv[]로 재구성) |
| [kernel-scheduler.md](kernel-scheduler.md) | ADR-014, 025, 027, 034, 035, 053, 055, 109, 124, 127, 133, 135, 137~139, 144, 176, 177, 178, 184~187, 191, 208, 209, 211, 212, 226 | 우선순위 밴드, NUMA 런큐, 승격, 도네이션, 최근 스케줄링 빈도 측정, 스레드 영구 종료(sched::exit), FPU/SIMD 컨텍스트 스위칭(lazy XSAVE/AVX로 개정, 실제 구현은 M11b), AP 기동(INIT-SIPI-SIPI) 구현 세부, 워크 스틸링 범위 정정(커널 밴드도 노드 경계를 넘음), fpu_save_area 별도 페이지 할당(64바이트 정렬), yield() enqueue 순서 버그 수정, TLB shootdown의 로컬 코어 invlpg 누락 수정, M21 완성(LAPIC 타이머 기반 선점형 스케줄링, BSP·ring3 한정)+TSS.RSP0 스레드별 분리 버그 수정, M22 완성(sys_process_kill 스케줄러 픽 타임 폐기+procsrv wait 자기 전용 endpoint 패턴), LAPIC 타이머 PIT/HPET 보정 전략(모든 타이머는 유저모드 진입 전 보정), 진짜 멀티코어 선점형 스케줄러 전략(코어별 g_current+AP의 유저 run_queue 참여), 커널 타이머 추상화(timer_source_interface+코어별 LAPIC 백엔드), signal 전달 전략(pending_signals+return-to-user 트램폴린, SIGKILL의 즉시 대기열 unlink), pthread 전략(기존 create_user_thread의 address_space 공유 재사용+futex), M33 완성(HPET 1순위/PIT 폴백으로 코어별 LAPIC 타이머 실측 보정+ticks_for()가 진짜 나눗셈을 하도록 수정해 OPEN-62 해소, timer_source_interface 도입은 M34로 미룸), M36 완성(완전한 signal 계층을 syscall 리턴 시점 한정으로 범위를 더 좁혀 구현 — IRETQ 경로/SIGCHLD 자동 전달/SIGKILL 즉시 unlink는 미구현으로 남겨 OPEN-65 재오픈, 새 syscall sys_yield 추가), M37 완성(pthread 최소 구현 — sys_thread_create+sys_futex, musl __clone을 순수 C 대체로 갈아 끼움, SYS_exit/SYS_exit_group 混同 회귀 수정, __lock/__unlock을 진짜 futex 기반으로 되돌림), M55 설계(계획 단계, ADR-226): SIGINT 하나만 SIG_DFL=진짜 종료로 하드코딩(나머지 31개 시그널은 ADR-211 그대로 무시 — OPEN-75 신규) |
| [boot-and-drivers.md](boot-and-drivers.md) | ADR-017, 026, 030, 037~041, 043, 046, 056, 057, 097, 111, 114, 117, 119, 120, 122, 123, 126, 129~131, 143, 146, 147, 150, 154, 156, 157, 158, 163, 164, 166, 173~175, 181, 192, 193, 196, 214, 219 | 부트 프로토콜, 디버그 콘솔, PCIe, 드라이버 로드맵, 콘솔/TTY 드라이버(상세 프로토콜은 기존 표준 채택), QEMU 검증용 PVH 직접 부팅, Multiboot2 파서 self-test 검증 전략, initrd 임베딩(개발 환경), .boot.bss 명시적 제로화, SYSCALL ABI 관례, initrun 링크 주소, 패닉 시 스택 백트레이스, ext4 로드맵/구현 범위, 입력장치(PS/2·USB) 로드맵, initrun 부팅: 부트 디바이스(initrd의 disk.cfg로 전달) 직접 마운트해 서비스 탐색·기동, TSS(RSP0) 최소 설정, boot_device_descriptor 필드, virtio-blk PCI BAR 커널 직접 배정+IOPB, M12 완성(virtio-blk 클라이언트+실제 부트 디스크 마운트+procsrv 골격), 스레드별 활성 I/O 포트 범위(IOPB 재설계), sys_map_phys(MMIO 캐패빌리티), M14 완성(devmgr ACPI/PCIe 열거+PS/2 자체테스트+USB xHCI 리셋), M15 완성(virtio-blk 첫 실제 유저 드라이버+devmgr BAR 재사용 일반화+부트 디바이스 배제, 디스크 손상 버그 기록), devmgr I/O BAR 배정을 전진 커서로 전환(M16이 드러낸 동시 배정 충돌 버그 기록), M17 콘솔/TTY 범위 좁힘(cfgsrv 없는 단일 TTY+VGA 텍스트 모드), M17 완성(콘솔/ps2/procsrv/login 배선, initrun 서비스 레지스트리 크기+ps2 폴링 예산 버그 기록), 실제 GRUB Multiboot2 부팅 최초 검증+레거시 8259 PIC 마스킹(IRQ0/#DF 벡터 충돌 버그 기록), initrun/devmgr arch_data_addr 실값 전달 수정+실제 UEFI(OVMF) 부팅 최초 구현(별도 PE32+ EFI 스텁, retf/스택 순서 버그 기록), M25 완성(virtio-net 드라이버+netsrv DHCP 왕복 자기테스트, ARP 없이 — vring 베이스 오프셋 누락+RX 버퍼 페이지 정렬 버그 기록), OPEN-51 방향 전환("커널 서버"는 initrun이 하드코딩 직접 실행+"유저 서비스"는 별도 systemd류 데몬이 관리, 두 계층 분리로 데몬 존재는 유지), OPEN-52 해소(서비스 준비완료 신호를 spawn 시점 전용 endpoint의 Call/Reply로 통일), 유저 서비스 관리자(svcmgr) 설계(유닛 모델+시작 절차+재부모화 대상 확정+컨트롤 프로토콜 개요), M40 완성(svcmgr 골격+재부모화 실제 트리거+준비완료 신호 실동작, spawn 시점 endpoint 프록시 CAN_RECV 추가·mc/*_client.h 5개 extern "C" 누락·depends= 버퍼 오버플로·procsrv 자기 미등록 등 진짜 버그 4건 발견·수정), M43 완성(계정별 유저 서비스 인스턴스 — 유닛 scope(system/per_account)+로그인 감시 백그라운드 스레드(M37 mc_thread_create 첫 실사용)+"유닛@계정" 컨트롤 프로토콜 주소 지정 확장, g_runtime 재진입 보호 스핀락 신설) |
| [filesystem.md](filesystem.md) | ADR-018, 044, 045, 047, 048, 050, 058, 059, 065, 080, 099~103, 128, 153, 162, 168, 172 | fd 라우팅, VFS 런타임 디렉토리 구성, 마운트 네임스페이스·오버레이, 유저 영역 FS(FUSE류), SWAPFS(파티션/파일 백엔드), 파일 잠금·공유 모드(파일 단위), FS 서버 공통 파일 신원(fs_node_id), M13 완성(fs-protocol 최소 버전+vfs/memfs 서버+procsrv VFS 클라이언트), M16 완성(FAT32/ext4 읽기전용 FS 서버+VFS 정적 마운트 테이블+fs-protocol v2의 pages[] 기반 OP_READ), fs-protocol v3(M18: OP_WRITE도 pages[] 기반+memfs 읽기/쓰기 커서+OP_OPEN 호출자 신원 필드), fs-protocol v4(M20: memfs OP_LIST) |
| [registry-decisions.md](registry-decisions.md) | ADR-060~064, 169, 197, 215 | 설정 리포지터리(cfgsrv) 서브시스템, M19 범위 좁힘(group 검증 제외+실제 VFS 영속화+프로토콜 9종+프로토콜-레벨 정수 핸들), 유저 서비스 유닛 레지스트리 스키마(@global/system/services, 새 프로토콜 없이 기존 모델 재사용), M41 완성(svcmgr가 실제로 이 테이블을 읽음+cfgsrv 값 저장 한도/영속화 버퍼가 너무 작아 생긴 실제 메모리 손상 버그 발견·수정, fs-protocol close 부재 OPEN-70 신규) |
| [libk.md](libk.md) | ADR-066~073(073은 076으로 대체), 076~077, 118 | libk(커널·서버 공용 프리스탠딩 코어 라이브러리): result/optional/span/intrusive_list/atomic/spinlock·ticket_lock·mcs_lock, 검증 전략, 전역 constexpr 초기화 요구사항 |
| [security-model.md](security-model.md) | ADR-074~075, 079, 081~090, 092~096, 098, 112, 165, 167, 171(대체됨→224), 194, 201, 206, 217, 218, 224, 227 | 프로세스 신뢰(trusted) 위임 체인, 유저 프로세스 권한 상승(sudo류)·ROOT 전권 모델, procsrv 계정 모델, jail/guest VFS·실행·시스템 가시성 격리 정책, badge 신원 전파, super badge 재위임 원천 차단, 신원 불변 원칙, 계정 생성·로그인·su/sudo 절차, 탈중앙화 신원 위임, 위임 타임아웃 재조정 규칙, 위임 기간 모드(기본값/명시적/영구), 위임 시 재인증 요구 규칙, 세션 stdio/TTY 상속, M17 로그인 인증 범위 좁힘(procsrv 최소 계정 저장소+OP_LOGIN, 세션 프로그램 스폰은 M20으로 미룸), M18 범위 좁힘(하드코딩 위임 테이블+jail을 guest로 단순화+실제 경로→ELF 로더), M20 범위 좁힘(로그인 후 셸 시작을 procsrv→셸 OP_START IPC 신호로 단순화, M53에서 대체됨), 위임의 명령 단위 세부 범위(delegation_entry에 허용 명령 목록 추가), M27 완성(procsrv 실제 process_entry 테이블+범용 proc_op::wait/kill, 자기주장 pid+비블로킹 폴링 wait로 범위 좁힘, OPEN-54 해소+OPEN-67 신설), M32 완성(procsrv pid를 self_register/fork_register로 procsrv가 직접 스폰하지 않은 일반 프로세스까지 확장, pid를 커널 스레드 필드에 저장해 execve()를 거쳐도 보존), M43 완성(스폰 시점 상속 핸들 badge 오버라이드를 처음 실제로 연결해 svcmgr↔procsrv 한 조합에 한해 "진짜 svcmgr" 검증 가능해짐, su/sudo와 분리된 좁은 목적의 계정별 유저 서비스 위임 테이블 신설 — OPEN-71/72/73 신규), M53 완성(로그인 성공 시 procsrv가 OP_START 핑 대신 msh를 실제 sys_process_spawn — ADR-171의 전제였던 "임의 ELF 전달 메커니즘 없음"이 이미 M32에서 무효화됐던 것을 뒤늦게 발견해 대체, userland/shell 완전 제거), M55 설계(계획 단계, ADR-227): "프로세스 그룹"의 실제 주체는 procsrv가 아니라 msh 자신이다 — procsrv는 fork_register된 자식의 thread_handle을 원천적으로 모른다는 것을 발견해 setpgid/getpgid 일반화를 포기, msh가 자기 자식 핸들로 직접 SIGINT 전달(OPEN-76 신규) |

ADR 번호는 파일이 나뉘어도 **프로젝트 전체에서 계속 순차 증가**한다
(파일별로 번호가 다시 시작되지 않는다). 새 결정을 추가할 때는 가장
최근 ADR 번호 + 1을 쓰고, 주제가 맞는 파일에 추가한다. 어느 파일에도
잘 맞지 않는 새로운 주제라면 새 파일을 만들고 이 표에 추가한다.

## 미결정 항목

| 문서 | 내용 |
|---|---|
| [open-items.md](open-items.md) | 모든 카테고리에 걸친 미결정(OPEN) 항목과 해결 이력 |

## 그 외 설계 문서

| 문서 | 내용 |
|---|---|
| [repo-layout.md](repo-layout.md) | minicore **소스 저장소 자체**의 디렉토리 트리, CMake 서브프로젝트 구성 (build-system.md의 결정을 구체화) |

**주의**: [repo-layout.md](repo-layout.md)(저장소 구조)와
[../spec/vfs-layout.md](../spec/vfs-layout.md)(minicore가 부팅한 뒤
보여주는 런타임 파일시스템)는 이름이 비슷하지만 완전히 다른
대상이다 — 혼동하지 않는다.
