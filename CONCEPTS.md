# minicore 설계 원칙 (CONCEPTS)

이 문서는 minicore를 만들 때 계속 반복해서 적용되는 **원칙과 사고방식**을
정리한다. 구체적인 구조(어떤 서버가 있고 무엇을 하는가)는
[ARCHITECTURE.md](ARCHITECTURE.md)를 본다. 개별 결정의 근거·대안·부작용은
`docs/design/*.md`의 ADR(현재 230건)이 정본이다 — 이 문서는 그 ADR들을
관통하는 "왜 항상 이런 식으로 판단하는가"를 뽑아 정리한 것이지, ADR을
대체하지 않는다.

## 1. 목표: 학습용 토이가 아니라 동작하는 범용 OS

minicore는 처음부터 "동작하는 시스템"을 목표로 한다(ADR-001). 이것이
갈라지는 판단마다 미치는 영향:

- 설계가 갈릴 때는 **확장 가능성**과 **서버 작성의 편의성**을 우선한다.
  성능은 "나중에 최적화 가능한 구조"만 확보해 두고, 초기엔 정확성이
  먼저다.
- 커널 API는 처음부터 다중 프로세스·다중 서버를 전제로 설계한다.
- 단순함이 필요할 때는 기능 자체를 빼는 대신 **커널 밖(유저랜드)으로
  민다** — 이 하나의 규칙이 이 프로젝트의 커널이 왜 이렇게 작은지를
  설명한다.

## 2. 무엇을 직접 만들고 무엇을 빌려오는가

경계선이 명확하다(ADR-005/006/007/008/049):

| 계층 | 정책 |
|---|---|
| 커널(IPC·스케줄러·주소공간·객체) | 직접 구현, 절대 서드파티 코드 없음 |
| 디바이스 드라이버 | 직접 구현 (코어 3종만 커널 내부: 타이머·인터럽트 컨트롤러·부트 초기 콘솔) |
| 시스템 서비스(VFS·FS·네트워크·프로세스 서버·devmgr·cfgsrv 등) | 직접 구현 |
| libc / POSIX 계층 | 기존 libc 포팅(현재 musl) — syscall 레이어는 직접 만든 우회 계층 |
| 셸·coreutils·애플리케이션 | 포팅 우선, 단 개념이 근본적으로 안 맞으면 대체 구현 허용(ADR-049) |

이 경계는 실제로 여러 번 시험대에 올랐다. BusyBox를 셸/coreutils로
도입하려다 이 저장소의 빌드 체계(clang 컴파일+`ld.lld` 직접 링크)와
근본적으로 안 맞고 호스트 도구 체인 문제까지 겹치자, ADR-049를 실제로
발동해 도입을 철회하고 `userland/msh`를 직접 작성했다(ADR-221) — "포팅이
목적이 아니라 유저랜드 완성이 목적"이라는 원래 근거가 실제로 작동한
사례다. 반대로 musl은 셸/coreutils보다 개념적으로 minicore와 더 잘
맞았기(POSIX syscall 계약만 지키면 됨) 때문에 대규모 패치 없이 끝까지
포팅에 성공했다(M27~M38, M51~M56).

## 3. 마이크로커널 철학: 커널은 최소 기반, 서버가 시스템이다

- 커널 안에는 IPC, 스케줄러, 객체/핸들 테이블, 페이지 할당자, 코어
  드라이버 3종만 있다. 파일시스템·네트워크·프로세스 의미론(fork/exec/
  signal/wait/pid)·설정 저장소·디바이스 드라이버는 전부 유저 프로세스다.
- POSIX 서버를 하나로 뭉치지 않고 역할별로 분해한다(Hurd류, ADR-008) —
  프로세스 서버(procsrv), VFS(vfs), 파일시스템(memfs/fat32/ext4),
  네트워크(netsrv)가 각각 독립 프로세스다. 이렇게 나누는 이유는
  성능이 아니라 **장애 격리**다: 파일시스템 서버 하나가 죽어도 다른
  파일시스템·네트워크·프로세스 관리는 영향받지 않는다.
- 이 선택은 "Hurd가 겪은 어려움을 그대로 인지하고 감수"한 것이다
  (ADR-008 §영향) — 특히 fork()가 여러 서버에 흩어진 fd 상태를
  자식에게 정확히 복제해야 하는 문제(§7 참고)는 실제로 이 프로젝트에서
  가장 자주 재등장하는 어려운 지점이었다.

## 4. IPC가 유일한 통신 수단이다

커널 객체는 프로세스별 **단순 핸들 테이블**의 인덱스로만 참조된다
(ADR-011). 공유 메모리를 임의로 열어주는 API는 없다 — 모든 프로세스 간
데이터 이동은 IPC(`Call/Reply` 동기, `Notification` 비동기, ADR-004)를
통과한다. 이것이 강제하는 것:

- **캐패빌리티 기반 보안이 자동으로 따라온다** — "이 핸들을 가지고
  있다"는 것 자체가 권한이다. 별도의 ACL 조회 없이 핸들 유효성만
  검사하면 된다.
- **프록시 위임과 cascade revoke**(ADR-023) — 다른 프로세스에게
  객체 접근을 내줄 때는 항상 원본의 부분집합 권한만 가진 새 프록시
  핸들을 만든다. 소유자가 원본을 닫으면 그 트리 전체(자손 프록시까지)가
  즉시 무효화된다 — "나중에 재위임해서 회수를 피해가는" 경로가
  구조적으로 불가능하다.
- **badge를 통한 위조 불가능한 신원 전파**(ADR-084) — 서버가 "누가
  호출했는가"를 알아야 할 때, 클라이언트의 자기 선언이 아니라 커널이
  프록시 발급 시점에 붙인 값을 그대로 신뢰한다. 다만 이 원칙이 실제로
  전체 시스템에 적용되기까지는 시간이 걸렸다(§8 참고).

## 5. 두 종류의 "확장하는 방법" — 항상 커널을 먼저 건드리지 않는다

새 기능이 필요할 때 이 프로젝트가 되풀이해 온 판단 순서:

1. **먼저 기존 IPC/syscall로 유저랜드에서 풀 수 있는가?** 대부분 그렇다.
   - musl의 진짜 syscall 계층을 붙일 때, 커널에 Linux syscall
     번호/ABI를 추가하는 대신 musl 자신의 `syscall_arch.h`를 패치해
     모든 syscall을 `libc/sysdeps/minicore/syscall_shim.c`로 우회시켰다
     (ADR-183) — 커널은 musl의 존재를 전혀 모른다.
   - 파이프(`pipe()`/`dup2()`)가 필요했을 때 커널에 파이프 객체를
     추가하지 않고 전용 서버 `servers/pipesrv`를 새로 만들었다
     (ADR-220, M51) — `mc_yield()`+재시도로 블로킹을 흉내내는 것도
     `mc_wait()`가 이미 쓰던 요령의 재사용이다.
   - 설정 저장소(cfgsrv), 유저 서비스 관리자(svcmgr)도 전부
     `libk`+`libmc`만 링크하는 순수 유저 프로세스로, 새 커널 객체 종류를
     추가하지 않고 기존 핸들/IPC 모델 위에서 동작한다.
2. **정말 커널이 알아야만 하는 것(스케줄링·메모리·인터럽트·타이밍)만
   syscall을 추가한다.** `sys_fork`/`sys_exec`/`sys_thread_create`/
   `sys_futex`/`sys_signal_send` 등은 이 경우다 — 유저랜드가 스스로
   구현할 수 없는 진짜 커널 상태(스레드 컨텍스트, 페이지테이블,
   스케줄러 큐)를 다루기 때문이다.

이 규율 덕분에 커널 코드 자체는 프로젝트 규모에 비해 계속 작게
유지된다 — 새 서버·새 유저랜드 기능이 늘어나도 커널 syscall 표는
드물게만 늘어난다.

## 6. 유저랜드는 명시적으로 층이 나뉜다

`mc`(구 `libmc`)가 이 계층 구조의 정본이다(ADR-132, ADR-200):

```
0단  커널 syscall ABI            (mc/syscall.h가 정본 — kernel/include/uapi.hpp는 폐지됨, M50)
1단  mc (libs/mc)                모든 syscall의 1:1 C 래퍼 + 서버별 프로토콜 클라이언트
2a단 minicore 네이티브 서버/앱    k(libk, C++ freestanding 유틸) + mc만 링크, libc 불필요
2b단 포팅된 libc(musl)           mc의 mc_* 호출로 연결되는 얇은 syscall shim
3단  포팅된 셸/coreutils/앱       2b의 libc만 본다 — mc를 직접 보지 않는다
```

- `mc`는 순수 C+어셈블러로만 작성한다(C++ 금지) — 이 라이브러리를
  커널(C++, `MC_LAND_KERNEL` 매크로로 구분)과 유저 양쪽에서 참조할 수
  있어야 하기 때문이다.
- 프로토콜 와이어 포맷의 단일 출처는 항상 `mc`의 C 헤더다. `docs/spec/*.md`
  의 프로토콜 의사코드는 사람이 읽는 설명일 뿐, 실제 레이아웃 정의는
  헤더가 정본이다 — 클라이언트/서버가 같은 헤더를 `extern "C"`로
  include하면 POD 구조체 레이아웃 일치가 컴파일 타임에 보장된다.
- `libk`(→`k`)는 이 계층 구조 밖이다 — 커널·시스템 서버가 함께 쓰는
  C++ freestanding 유틸(`result`/`optional`/`span`/`intrusive_list`/
  `atomic`/락 종류)일 뿐, syscall이나 IPC 프로토콜과는 무관하다.

## 7. fork()가 마이크로커널에서 왜 어려운가, 그리고 그 답

POSIX 유저랜드를 포팅 대상으로 삼은 순간(ADR-005) `fork()`는 이
프로젝트에서 가장 자주 재등장하는 어려운 문제가 됐다 — 자식 프로세스는
부모가 여러 서버(파일시스템, 파이프, 네트워크)에 흩어 놓은 열린 상태를
전부 이어받아야 한다. 이 문제에 대한 이 프로젝트의 답은 일관된 패턴을
따른다:

- **각 fd가 속한 서버가 "복제" 연산에 직접 참여한다** — 커널의 일반
  프록시 위임만으로는 부족하다. 프록시는 "같은 객체를 가리키는 rights
  축소 뷰"만 만들 뿐, 파일의 읽기/쓰기 오프셋처럼 서버가 자신의 상태로
  들고 있는 값까지 부모·자식이 공유하게 만들지는 않기 때문이다
  (`procsrv.md` §4.1).
- **참조 카운트 계약은 "누가 소유권을 이동시키는가"를 명확히 해야
  한다** — 이 계약을 잘못 설계하면 조용히 심각한 버그가 된다. M51이
  파이프에 설계한 "dup" 계약(fork가 fd 테이블을 복제한다는 전제)이
  M54의 msh 파이프라인(양끝을 들고 있다가 각기 다른 자식에게 정확히
  한 번씩 **넘겨준다**)과 안 맞아, 무한 대기 버그와 "성공한 것처럼
  보이지만 실제로는 파이프가 사라지는" 버그가 연쇄로 발생했다 — 최종
  해법은 명시적 "이동" 모델(`mc_shell_forget_pipe_fd`)로 계약 자체를
  정정하는 것이었다(ADR-225).

## 8. 신뢰와 격리는 위임 체인이며, 되도록 늦게 정한다

- **`trusted`/`confinement_tier`(guest/jail)는 프로세스 생성 시점에
  정확히 한 번만 정해지고 이후 절대 바뀌지 않는다.** "신원을 바꾸고
  싶으면 항상 새 프로세스를 만든다"는 불변 원칙이다 — 기존 프로세스를
  제자리에서 승격/강등시키는 경로는 아예 없다.
- **권한은 커널이 최초 한 명(initrun)에게만 부여하고, 그 뒤로는 전부
  위임 체인이다.** 커널→initrun(무조건 trusted)→initrun이 필요한 서버
  (cfgsrv 등)에게만 재위임 — "누가 이 권한을 원래 가졌는가"를 추적하면
  항상 이 체인 하나로 귀결된다. su/sudo도 같은 정신의 확장이다 —
  대상 계정이 미리 등록해 둔 위임 테이블을 조회하는 것으로 풀며, 별도의
  전역 sudoers 목록을 두지 않는다.
- **badge 기반 신원 검증은 처음부터 전체 시스템에 강제되지 않았다** —
  이것이 이 프로젝트의 정직한 한계 중 하나다. 초기 여러 프로토콜
  (VFS의 guest/jail 신원, cfgsrv의 uid)은 "호출자의 자기 선언"으로
  시작했고, badge 기반 위조 방지는 실제로 필요해진 지점(M43의 svcmgr↔
  procsrv 조합)에만 좁게 연결됐다(ADR-217) — 나머지는 여전히 OPEN
  항목(`docs/design/open-items.md`)으로 정직하게 남아 있다.

## 9. 문서와 결정 기록의 규율

- **spec/plan/done/design/remind는 서로 다른 시제를 갖는다** —
  `spec`은 "무엇을 만들 것인가"(구현 가능한 수준), `plan`은 "실행
  전"의 계획만, `done`은 "실행 완료"만 기록한다. `plan` 문서가
  실행되면 그 내용은 지워지지 않지만 `done`에 결과가 별도로 남는다.
- **ADR은 절대 사후 수정하지 않는다** — 결정을 번복하면 새 ADR을
  추가하고 옛 ADR에 `상태: 대체됨 (→ ADR-NNN)`만 표시한다. 결정의
  역사(무엇을 왜 바꿨는가)가 코드 리뷰만큼 중요한 산출물이라는
  전제다. ADR 번호는 파일이 여러 개로 나뉘어도 프로젝트 전체에서
  하나의 순차 번호(2026-09-11 기준 230)를 공유한다.
- **미결정 사항은 숨기지 않고 `OPEN-N`으로 명시한다** — "여기까지만
  하고 나머지는 미룬다"는 판단을 내릴 때마다(대부분의 마일스톤이 그렇다)
  그 갭을 OPEN 항목으로 등록해 나중에 다시 찾을 수 있게 한다.
- **검증 결과는 정직하게 보고한다** — 실제로 확인하지 못한 것을
  확인했다고 기록하지 않는다. "컴파일러가 없어 실패까지만 확인했다"는
  것과 "정상 동작을 확인했다"는 것을 절대 섞어 쓰지 않는다.

## 10. 검증 철학: 실행이 설계를 검증한다

- **QEMU 회귀 스위트가 항상 최종 심판이다.** 스모크(부팅+주요 자기
  테스트 문자열 매칭)/SMP/NUMA/AVX/네트워크 5개 스위트를 모든 유의미한
  변경 뒤에 전부 돌린다 — 문서가 아무리 정교해도 QEMU가 패닉을 내면
  그 설계는 틀린 것이다.
- **실행하며 발견한 버그가 설계 문서 자체를 갱신한다.** 이 프로젝트의
  ADR 상당수는 "계획을 세우기 전에 예측한 문제"가 아니라 "실제로
  QEMU에서 재현한 버그를 고치며 알게 된 것"을 기록한다 — 예를 들어
  이 저장소 역사상 첫 진짜 커널 동시성 버그(`handle_table` 무동기화
  +IPC 페이지 매핑 슬롯 공유)는 10년 가까이(M11부터) OPEN 항목으로만
  존재하다가, M56에서 pthread 기반 병렬 빌트인을 실제로 동시 실행하는
  첫 시나리오가 나타나고서야 실제 데이터 손상으로 드러났다(ADR-229).
  단일 스레드/단일 코어로는 코드 리뷰만으로 절대 잡을 수 없던 버그였다.
- **범위를 좁혀서라도 실제로 동작하는 것을 만든다.** 계획이 예상보다
  어려우면(예: musl 자신의 동적 링커 `libc.so`를 자기재배치까지 포함해
  포팅하는 것) 계획 문서 자신이 미리 정해 둔 "크게 막히면 되돌아간다"
  조항을 실제로 발동해 범위를 좁힌다(ADR-203, 정적 링킹으로 복귀). 이
  패턴은 M17~M20, M27~M38, M51~M56 등 거의 모든 계획에서 반복된다 —
  "완벽한 설계를 먼저 완성한 뒤 구현"이 아니라 "좁은 범위로 실제로
  동작시킨 뒤, 남은 갭을 OPEN으로 명시하고 다음으로 넘어간다".

## 11. 요약: 판단이 갈릴 때 되묻는 질문들

- 이걸 커널에 넣어야만 하는가, 아니면 유저랜드 서버로 풀 수 있는가?
  → 대부분 후자다.
- 이 기능은 처음부터 있어야 하는가, 아니면 실제로 필요해질 때까지
  미뤄도 되는가? → 미룰 수 있으면 `OPEN-N`으로 등록하고 미룬다(YAGNI).
- 이 소프트웨어는 포팅할 것인가, 새로 쓸 것인가? → 기본은 포팅,
  개념이 근본적으로 안 맞으면 대체 구현(ADR-049).
- 이 권한/신원 변경은 기존 프로세스를 제자리에서 바꾸는가? →
  그렇다면 잘못된 설계다. 항상 새 프로세스를 만든다.
- QEMU로 실제로 확인했는가? → 확인하지 않았다면 "확인했다"고 쓰지
  않는다.
